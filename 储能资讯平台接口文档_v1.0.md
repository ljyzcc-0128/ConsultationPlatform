# 储能资讯平台接口文档 v1.0

> 更新日期：2026-09-20 ｜ 服务：ums-main-java（:8080）、ums-crawler（:8100）

## 1. 全局约定

### 1.1 认证方式

| 模式 | 说明 |
|------|------|
| 无认证 | 白名单路径直接访问（新闻查询、搜索、内容详情） |
| Bearer Token | `Authorization: Bearer <JWT>`，登录获取，有效期可配置 |
| ADMIN 角色 | JWT 中携带 `ADMIN` 角色，才能访问 `/api/v1/admin/**` |

### 1.2 权限分层（SecurityConfig）

| 路径模式 | 权限 | 设计理由 |
|----------|------|----------|
| `/api/auth/login`、`/api/news/**`、`/api/v1/search`、`/api/v1/contents/**` | permitAll | 内容消费接口公开，支持未登录浏览模式，降低使用门槛 |
| `/api/me/**`、`/api/v1/supply-chain/**` | authenticated | 个人数据与看板涉及内部数据口径，需登录 |
| `/api/v1/admin/**` | hasRole(ADMIN) | 管理操作集中在一个前缀下，一处规则统一收口，避免遗漏 |
| `/internal/**` | 仅内网调用 | 内部运维接口不暴露在 `/api` 公共前缀下，与外部接口物理隔离 |

### 1.3 通用响应约定

- 时间统一 UTC ISO 8601（如 `2026-09-20T15:01:58`），前端按用户时区转换展示
- 枚举值返回前端使用英文大写（如 `MAJOR`、`HIGH`、`SUCCESS`）
- 业务错误通过 HTTP 状态码表达：`400` 参数错误、`401` 未认证、`403` 无权限、`404` 不存在、`409` 状态冲突、`502` 下游服务不可用

---

## 2. 认证接口（AuthController）

### POST /api/auth/login — 用户登录

| 项 | 内容 |
|----|------|
| 请求体 | `{"provider": "LOCAL\|IAM", "username": "admin", "credential": "***"}` |
| 返回 | `{"token": "<JWT>", "user": {...}}` |
| 权限 | 公开 |

**功能**：验证用户名口令，签发 JWT。provider 可选 `LOCAL`（本地验证）或 `IAM`（转发公司内部 IAM API 验证）。

**设计理由**：登录必须用 `POST`——凭证属于敏感数据，不能出现在 URL 或缓存日志中；同时登录是非幂等操作（会产生 token 签发这一副作用）。provider 字段让本地开发和公司 IAM 接入共用同一端点，切换无需改前端路由。

---

## 3. 内容查询接口（公开）

### GET /api/news（同 /api/v1/contents）— 资讯列表

| 项 | 内容 |
|----|------|
| 参数 | `cursor`、`limit`、`source`、`category`、`language`、`from`、`to`、`status`（均可选） |
| 返回 | `{ items: [...], nextCursor }` |
| 权限 | 公开 |

**功能**：分页查询资讯列表，支持按来源、分类、语言、日期区间、审核状态筛选。

**设计理由**：
- 查询用 `GET`：幂等、可缓存、参数直观可分享；
- 采用 **cursor 游标分页**而非 offset：资讯流按 `publish_time` 排序，游标分页避免深分页时的大偏移扫描，且新内容插入不会造成翻页重复/漏条；
- 双路径是刻意保留：`/api/news` 为前端既有公共路径，`/api/v1/contents` 为 v1 契约收口路径，两者同源实现，避免前端一次性迁移风险。

### GET /api/news/{articleId} — 资讯详情

**功能**：返回单篇资讯完整字段（含 AI 三段式摘要、分类、原文链接）。RESTful 路径参数直接标识资源，`GET` 单资源是最典型的资源读取语义。

### GET /api/v1/search — 全文搜索

| 项 | 内容 |
|----|------|
| 参数 | `q`（关键词）、`cursor`、`limit` |
| 返回 | `{ items: [...], nextCursor }` |
| 权限 | 公开 |

**功能**：基于 MySQL ngram 全文索引的中文/英文关键词搜索，返回带摘要的匹配列表。

**设计理由**：搜索参数放 query string 而非 body——搜索引擎的 URL 可直接分享/收藏；同样用 cursor 分页保证结果集变化时的翻页一致性。

### GET /api/v1/contents/{id}/related — 相关内容

**功能**：基于关键词与分类重合度，返回与指定内容相关的其他资讯。挂在资源路径 `/contents/{id}` 之下（子资源模式），直观表达"这条内容的关联内容"。

### GET /api/v1/contents/{id}/timeline — 事件时间线

**功能**：返回该内容主题的事件演化线（首次检测 → 热度上升 → 重大变化 → 稳定跟踪）。同为 `{id}` 的子资源，`GET` 只读。

---

## 4. 我的情报（需登录，FeedController / SubscriptionController）

### GET /api/me/feed?tab=today|unread|subscriptions|hot — 个人资讯流四 Tab

**功能**：今日（24h 内最新）、未读（个人已读状态过滤）、订阅（命中用户订阅规则）、热门（互动/热度排序）四个视图合一端点。

**设计理由**：
- 路径用 `/api/me` 而非 `/api/users/{userId}`——用户身份从 JWT 解出而非 URL 传入，**杜绝越权读取他人 Feed**（水平越权防护）；
- 四个 Tab 合并为一个端点 + `tab` 参数：四个视图本质是同一数据集的不同投影，避免 4 个端点的重复实现，前端切换 Tab 只改一个参数。

### POST /api/me/read-status/batch — 批量标记已读

**请求体**：`{"articleIds": ["id1", "id2", ...]}`

**设计理由**：用 `POST` 批量提交而非逐条 PUT——用户"全部标为已读"是一次性提交几十条 ID，逐条请求会产生 N 次网络往返；批量语义下 `POST` 更贴合"提交一批变更"的动作。

### POST /api/me/read-status/reset — 重置全部已读

**设计理由**：这是"动作"而非"资源替换"，没有天然的资源 ID 可供 PUT 定位，用 POST + 明确动词路径表达操作意图。

### GET /api/me/subscriptions — 我的订阅列表
### POST /api/me/subscriptions — 新建订阅规则
### PUT /api/me/subscriptions/{id} — 更新订阅规则
### DELETE /api/me/subscriptions/{id} — 删除订阅规则

**功能**：订阅规则的完整 CRUD（关键词/分类/来源组合条件，供匹配引擎推送）。

**设计理由**：标准 REST 资源设计——`GET` 列表、`POST` 创建（非幂等，返回 201）、`PUT /{id}` 整体更新（幂等，客户端重试安全）、`DELETE` 删除。userId 一律从 token 推导，Service 层做归属校验，防止用户改到别人的订阅。

---

## 5. 供应链看板（需登录，SupplyChainController）

### GET /api/v1/supply-chain/materials — 原材料市场指标
**返回**：`{ indicators: [{code, name, unit, latestValue, momChange, points[30天]}] }`
**功能**：6 项材料指标（碳酸锂、正极、电解液、铜箔、电芯、采购指数）及 30 天时序。

### GET /api/v1/supply-chain/inventory — 供需与库存
**返回**：`{ months: [...], series: [{name, type: bar|line, unit, data[]}] }`
**功能**：库存组合图数据（社会库存/仓单柱状 + 排产折线）。

### GET /api/v1/supply-chain/trade-logistics?status=&q= — 贸易与物流
**参数**：`status`（在途/已到港/清关中/已完成）、`q`（货物关键字），均可选
**功能**：提单记录筛选查询，含展开的时间线事件。

### GET /api/v1/supply-chain/suppliers — 供应商动态
**返回**：`{ updates: [{supplierName, updateType, riskLevel, date, description}] }`

**设计理由**：
- 四个区块各自独立端点而非一个大接口——四个图表的数据量、更新频率、前端缓存策略（staleTime）不同，独立端点让前端按需加载、独立缓存与重试；
- `trade-logistics` 的筛选走 query string：筛选条件是只读查询的一部分，`GET + 参数` 语义清晰且可缓存；
- months 与 series 的组合图数据一次返回（而非分两次取坐标轴与序列），保证图表渲染的原子一致性。

---

## 6. 管理后台（需 ADMIN 角色）

### 6.1 信源管理（AdminSourceController）

| 接口 | 功能 | 设计理由 |
|------|------|----------|
| GET /api/v1/admin/sources | 全量信源列表（按优先级+ID 排序） | 只读查询用 GET |
| POST /api/v1/admin/sources | 新增信源（必填 ID/名称/URL/语言，其余默认），返回 201 | 创建非幂等；重复 ID 返回 400 由 Service 校验 |
| PUT /api/v1/admin/sources/{sourceId} | 更新可编辑字段（启停/频率/优先级/名称/备注） | 整体更新幂等，网络重试不会产生副作用 |
| POST /api/v1/admin/sources/{sourceId}/trigger?limit=10 | 手动触发一次采集 | **动作型端点**：trigger 不是资源 CRUD 而是触发异步任务，用 POST + 动词子路径；不 PUT 是因为触发不修改信源资源本身 |

### 6.2 采集任务（AdminTaskController）

| 接口 | 功能 | 设计理由 |
|------|------|----------|
| GET /api/v1/admin/tasks?sourceId= | 采集任务历史（可按信源过滤），按开始时间倒序 | sourceId 为可选 query 参数——省略时返回全部，传值时过滤，一个端点两种用法 |

### 6.3 审核队列（AdminReviewController）

| 接口 | 功能 | 设计理由 |
|------|------|----------|
| GET /api/v1/admin/reviews | 待审核列表 | 只读 |
| POST /api/v1/admin/reviews/{articleId}/approve | 通过审核 | 审核是**状态迁移动作**（PENDING→APPROVED），非幂等的业务动作，POST 最贴切；路径挂具体文章 ID，权限与审计都能定位到对象 |
| POST /api/v1/admin/reviews/{articleId}/reject | 驳回 | 同上 |
| PUT /api/v1/admin/reviews/{articleId}/category | 修正分类 | 分类是文章的一个字段属性，PUT 表达"设置该字段的值"，幂等 |

### 6.4 死信队列（AdminDlqController）

| 接口 | 功能 | 设计理由 |
|------|------|----------|
| GET /api/v1/admin/dlq | 队列概览（各队列堆积数） | 只读 |
| GET /api/v1/admin/dlq/{queue}/messages | 查看某队列死信明细 | 队列作为路径参数的子资源 |
| POST /api/v1/admin/dlq/{queue}/requeue | 死信重新入队 | requeue 是动作（可能重复消费），POST；幂等性上与业务约定"可重复执行"由消息 ID 去重兜底 |
| DELETE /api/v1/admin/dlq/{queue} | 清空队列 | DELETE 直接对应删除语义 |

### 6.5 用户管理（UserController）

| 接口 | 功能 | 设计理由 |
|------|------|----------|
| GET /api/v1/admin/users | 用户列表 | 只读 |
| POST /api/v1/admin/users | 创建用户（返回 201） | 创建非幂等 |
| PUT /api/v1/admin/users/{id} | 更新用户（角色/状态） | 幂等更新 |
| DELETE /api/v1/admin/users/{id} | 删除用户 | 删除语义，且审计切面会记录操作日志 |

### 6.6 推送任务（PushAdminController）

| 接口 | 功能 | 设计理由 |
|------|------|----------|
| GET /api/v1/admin/push | 推送任务列表 | 只读 |
| POST /api/v1/admin/push/{id}/toggle | 启停切换 | toggle 是典型的动作型操作（每次调用效果相反，**天然非幂等**），必须用 POST 而非 PUT |

### 6.7 调度状态（SchedulerStatusController）

| 接口 | 功能 | 设计理由 |
|------|------|----------|
| GET /api/v1/admin/scheduler/status | 调度器状态（触发上限/到期信源/启用数） | 纯状态查询，前端 30s 轮询刷新，GET 可被中间层缓存与重试 |

### 6.8 审计日志（AdminAuditController）

| 接口 | 功能 | 设计理由 |
|------|------|----------|
| GET /api/v1/admin/audit-logs?action=&objectType=&limit=&offset= | 审计记录查询 | 只读 + 可选过滤；日志是 append-only 数据，用 limit/offset 简单分页即可（数据按 logId 有序且不会变更，offset 分页无游标分页的一致性问题） |

---

## 7. 内部接口（仅内网）

### POST /internal/ai/reprocess?articleId= 或 ?pendingOnly=true — AI 补加工

**功能**：对指定文章或所有 pending 文章重新执行 AI 摘要/分类/翻译加工。

**设计理由**：挂在 `/internal` 而非 `/api` 前缀——运维补偿接口，不应对外网暴露；用 POST 因为补加工会写库产生副作用。按 articleId 单条与 pendingOnly 批量两种模式合一，复用同一入口。

### ums-crawler（Python 服务，:8100）

| 接口 | 功能 | 设计理由 |
|------|------|----------|
| GET /healthz | 健康检查 | 探活专用路径，无业务逻辑 |
| POST /internal/trigger/{source_code} | 触发单信源抓取 | Java 主服务调 Python 采集服务的内部 RPC；触发采集是副作用动作，POST |

---

## 8. HTTP 方法选型总原则

| 方法 | 语义 | 本项目用于 | 幂等性 |
|------|------|-----------|--------|
| GET | 读取资源/查询 | 所有列表、详情、看板、状态查询 | ✅ 可缓存可重试 |
| POST | 创建资源 / 触发动作 | 登录、创建信源/用户/订阅、trigger、approve/reject、toggle、requeue、批量已读 | ❌ |
| PUT | 整体更新资源字段 | 信源配置更新、订阅更新、分类修正、用户更新 | ✅ 重试安全 |
| DELETE | 删除资源 | 删除用户/订阅/清空队列 | ✅ |

**核心决策规则**：
1. **只读即 GET**——查询绝不产生副作用，让浏览器缓存、CDN、重试机制都能安全介入；
2. **动作型端点（trigger/approve/toggle/requeue）用 POST + 动词路径**——它们表达"执行一个业务动作"而非 CRUD，强行映射成 PUT 反而语义扭曲；
3. **创建用 POST、更新用 PUT**——区分幂等性：PUT 重试无副作用，POST 重复提交会被唯一性校验拦截（如重复 sourceId 返回 400）；
4. **身份从 token 推导，资源 ID 从路径取**——`/api/me/*` 不接受 userId 参数，防止水平越权；
5. **管理接口统一 `/api/v1/admin/**` 前缀**——RBAC 规则一处收口，新增管理接口自动被保护。
