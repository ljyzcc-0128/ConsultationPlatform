# 储能资讯平台一期 · 整体项目规划与开发文档

> 版本：v1.2（整体规划版，新增资源配置清单）
> 编写日期：2026-09-20
> 规划口径：本版按**从零建设**进行整体规划，不对照任何既有实现进度；全部功能均为待建设范围
> 团队配置：2 开发（1 后端/采集 + 1 前端）、1 测试、1 产品经理
> 人天口径：**开发 35 人天 + 测试 5 人天 + 产品 2 人天 = 项目合计 42 人天**
> 事实依据：技术栈选型以仓库既有约定（package.json / pom.xml / requirements.txt 口径）为准

---

## 目录

- [一、项目总览](#一项目总览)
- [二、团队组织与角色分工](#二团队组织与角色分工)
- [三、服务器搭建方案](#三服务器搭建方案)
- [四、资源配置清单](#四资源配置清单)
- [五、需求范围与系统设计](#五需求范围与系统设计)
- [六、总体排期与人天](#六总体排期与人天)
- [七、测试计划](#七测试计划)
- [八、产品验收](#八产品验收)
- [九、附录](#九附录)

---

## 一、项目总览

### 链路图

![image-20260921132853973](/Users/jinxiansen/Library/Application Support/typora-user-images/image-20260921132853973.png)

```
【触发层】
  XXL-JOB 调度中心 :8081 ──每日回调──▶ CrawlScheduler @XxlJob(crawlScheduledJob)
  管理后台 Sources 页   ──POST /api/v1/admin/sources/{id}/trigger──▶ CrawlTriggerClient
                                        │ HTTP POST /internal/trigger/{source_code}
                                        ▼
【采集层】Python ums-crawler-py :8100（FastAPI）
  trigger.py → Runner.run() → selectors/*.yaml
      → Connector（WebPage / ApiSource / INT-03 Playwright）
      → parsing（date_parser 缺日期→null · url_utils 去 utm_* · fallback 正文）
      → mq/publisher.py
          ├─ RabbitMQ 可达   → 发布 raw.item.fetched
          └─ RabbitMQ 不可达 → 降级 stdout 日志
                                        │
                                        ▼
【消息层】RabbitMQ :5672 · exchange ums(topic) · 死信 exchange ums.dlx
  raw.item.fetched  → [ums.raw.item.fetched.queue]   ──重试3次失败──▶ ums.dlx → *.dlq
  content.parsed    → [ums.content.parsed.queue]     ──重试3次失败──▶ ums.dlx
  content.processed → [ums.content.processed.queue]  ──重试3次失败──▶ ums.dlx
                                （DLQ 重放：管理后台 DlqPage /requeue · replay_dlq.py）
  content.finalized ⚠️ 已发布、未声明队列（预留推送/索引入口）
                                        │
                                        ▼
【加工层】Java ums-main-java :8080 消费者链
  ① @RabbitListener raw.item.fetched → NewsIngestService
       幂等校验（URL hash + 内容指纹）→ 写 MySQL → 发布 content.parsed
  ② @RabbitListener content.parsed → AiEnrichService → glm-4-flash
       （UMS_AI_API_KEY；超时/限流 → 降级不阻断）→ 发布 content.processed
  ③ @RabbitListener content.processed → DuplicationService
       （指纹精确 / 标题归一 / 相似+日期差）→ 归组 → 发布 content.finalized
                                        │
                                        ▼
【存储层】MySQL :3306 · cp_news 业务库 + xxl_job 调度库（Flyway V1~V6）
                                        ▲
【查询层】Java REST :8080（16 个 Controller，明细见 2.5 节）
                                        ▲
                                        │ HTTP /api（dev 代理；生产 nginx 80/443 反代）
【展示层】Web 前端 ums-web :5173
  登录 /api/auth/login（Local + IAM 双模式）· 我的情报 /api/me/feed
  订阅 /api/me/subscriptions · 情报中心 /api/v1/contents（游标分页）
  详情 /api/v1/contents/{id}（+ related / timeline）· 搜索 /api/v1/search
  供应链 /api/v1/supply-chain/* · 管理后台 /api/v1/admin/*（7 页）
```



### 1.1 系统定位

将分散在 31 个候选信源中的储能政策、行业动态、供应链行情，统一**采集 → AI 加工 → 去重聚合 → 查询展示 → 订阅推送**，形成结构化、可检索、可订阅的信息资产。

### 1.2 核心目标

- 一期接入 5 个试点信源（3 中文 + 2 英文），跑通采集到推送的全链路；
- 建成用户端三入口（我的情报 / 情报中心 / 供应链看板）与管理后台；
- 具备用户体系、订阅推送、全文检索、指标看板能力；
- 单机部署上线，满足每日 1 次的采集频率。

### 1.3 三层架构

```
┌────────────────────────────────────────────────────────────────┐
│  Web 前端 ums-web（React 18 + TS + Vite，端口 5173）            │
│  我的情报 │ 情报中心 │ 供应链看板 │ 管理后台                      │
└──────────────┬─────────────────────────────────────────────────┘
               │ HTTP/REST（dev 代理 /api → 8080）
┌──────────────┴─────────────────────────────────────────────────┐
│  Java 主服务 ums-main-java（Spring Boot 3.3.5，端口 8080）       │
│  内容查询 │ AI 加工 │ 去重聚合 │ 管理后台 │ XXL-JOB 执行器(9999) │
└──────┬───────────────────────────────────┬─────────────────────┘
       │ RabbitMQ 异步消息（5672）          │ HTTP 触发采集
┌──────┴───────────────────────┐  ┌────────┴─────────────────────┐
│  RabbitMQ 4.3.6              │  │  Python 采集 ums-crawler-py   │
│  exchange: ums（topic）       │◄─┤  FastAPI（8100）+ Playwright  │
│  管理台 15672                 │  └──────────────────────────────┘
└──────────────────────┘
       ▲
┌──────┴─────────────────────────────────────────────────────────┐
│  MySQL（cp_news 业务库 / xxl_job 调度库）+ Flyway 版本化迁移     │
└────────────────────────────────────────────────────────────────┘
```

### 1.4 核心设计原则

| 原则 | 落地方式 |
|---|---|
| 采集不直落库 | Python 只发 MQ 消息 `raw.item.fetched`；无 MQ 时降级 stdout |
| 单一调度系统 | 只有 XXL-JOB 一套调度，Python 侧不引入调度器，只暴露 HTTP 触发接口 |
| AI 与业务解耦 | AI 适配层接口 + 环境变量注入 key；AI 故障不阻断入库 |
| 审核统一 | 一套 `manual_review_status`，未审内容不对外展示，不做两套审核流程 |
| 契约先行 | 前后端以接口契约为界并行开发；MQ 消息契约版本化管理 |
| 敏感配置 | 数据库密码、AI key、MQ 密码全部走环境变量，不入代码仓库 |

### 1.5 端口规划

| 服务 | 端口 | 公网是否开放 |
|---|---|---|
| nginx（HTTP/HTTPS） | 80 / 443 | ✅ 是 |
| Web 前端 Vite dev | 5173 | ❌ 仅开发机 |
| Java 主服务 REST | 8080 | ❌ 仅本机/内网 |
| XXL-JOB 执行器 / 调度中心 | 9999 / 8081 | ❌ 仅本机/内网 |
| Python 采集服务 FastAPI | 8100 | ❌ 仅本机/内网 |
| RabbitMQ AMQP / 管理台 | 5672 / 15672 | ❌ 仅本机/内网 |
| MySQL | 3306 | ❌ 仅本机/内网 |

---

## 二、团队组织与角色分工

### 2.1 角色与职责

| 角色 | 主要职责 | 人天投入 |
|---|---|---|
| 产品经理（PM） | 需求文档与原型、信源范围确认、验收标准（AC）定义、UAT 执行与验收签字、缺陷优先级裁定 | 2 人天 |
| 后端/采集开发（开发A） | 服务器搭建、Python 采集服务、Java 主服务、用户/推送/指标等后端功能、端到端联调 | 27.5 人天 |
| 前端开发（开发B） | ums-web 全部页面、接口对接、前端单测、参与联调与缺陷修复 | 7.5 人天 |
| 测试工程师（QA） | 测试计划与用例、接口/集成/系统/兼容性/回归测试、缺陷跟踪、UAT 支持 | 5 人天 |
| 运维支持 | 云主机/域名/安全组由后端开发兼任实施（工时已含在服务器搭建中） | — |
| **合计** | | **42 人天** |

### 2.2 协作机制

- **每日站会**：15 分钟，同步进度/阻塞/当日计划；
- **评审节点**：需求评审（开工前）、契约评审（阶段 2 末）、提测评审（feature freeze）、验收评审（UAT 后）；
- **缺陷管理**：统一在缺陷看板登记，按 P0-P3 分级（定义见第七节），P0/P1 当日响应；
- **文档同步**：接口契约、环境变量、部署步骤随代码变更即时更新，以本文档为唯一规划基线。

---

## 三、服务器搭建方案

一期采用**单机部署**（Linux 云主机 + Docker），承载全部三层服务与中间件；后续可根据负载水平拆分。

### 3.1 服务器规划

| 项 | 建议 |
|---|---|
| 主机规格 | 4 vCPU / 8 GB 内存 / 100 GB SSD |
| 操作系统 | Ubuntu 22.04 LTS（时区统一 UTC） |
| 公网带宽 | 5 Mbps（固定/按量带宽按实际访问量选择） |
| 部署方式 | MySQL / RabbitMQ / XXL-JOB 调度中心使用 Docker Compose；Java、Python 服务以容器或 systemd 常驻；前端 dist 由 nginx 托管 |
| 安全组 | 仅放行 22（SSH）、80、443；3306 / 5672 / 8080 / 8100 / 9999 / 8081 仅限本机或内网访问 |

### 3.2 部署拓扑

- **nginx（80/443）**：静态托管 ums-web 构建产物；`/api` 反代 → Java 8080；
- **Java 主服务（8080）**：业务 REST、管理后台接口、XXL-JOB 执行器（9999）；
- **Python 采集服务（8100）**：只接收 Java / XXL-JOB 的内部触发调用，不对公网开放；
- **Docker 容器**：MySQL 8（cp_news / xxl_job 两个库）、RabbitMQ 4（5672 / 管理台 15672）、XXL-JOB 调度中心 2.4.0（8081）。

### 3.3 搭建步骤

1. **主机初始化**：创建部署用户与 SSH key、安全组仅放行 22/80/443、设置时区 UTC、安装 Docker Engine 与 compose 插件；
2. **中间件启动**：compose 启动 MySQL 8（utf8mb4 / utf8mb4_0900_ai_ci）、RabbitMQ 4（**禁用 guest 默认账号**，创建最小权限专用账号与 vhost）、XXL-JOB 调度中心（初始化 xxl_job 库 8 张表，默认 admin/123456 首登立即改密）；
3. **数据库初始化**：创建 `cp_news` 业务库；由 Flyway 在 Java 服务首次启动时自动建表并写入 5 条信源种子，无需手工执行 SQL；
4. **MQ 初始化**：预建 durable topic exchange `ums` 与死信 exchange `ums.dlx`，业务队列由 Java 服务启动时自动声明；
5. **服务部署**：打包 Java jar 与 Python 依赖；通过环境变量注入数据源/MQ 连接串、`UMS_AI_API_KEY`、`UMS_ADMIN_TOKEN`（清单见附录 9.4）；以 compose 或 systemd 常驻并设置开机自启；
6. **前端与反代**：`npm run build` 产物部署至 nginx；配置 `/api → 8080` 反代、CORS 白名单按生产域名收紧、签发 Let's Encrypt SSL 证书；
7. **调度与冒烟验证**：在 XXL-JOB 调度中心配置每日 1 次的采集任务；依次验证健康检查、首页加载、手工触发一次全链路成功。

### 3.4 搭建工期（开发工时，2 人天）

| 工作项 | 工期 |
|---|---|
| 主机初始化 + Docker 环境 | 0.5 人天 |
| 中间件部署（MySQL / RabbitMQ / XXL-JOB） | 0.5 人天 |
| 数据库与 Exchange 初始化 | 0.25 人天 |
| 三服务部署与环境变量配置 | 0.5 人天 |
| nginx / SSL 与冒烟验证 | 0.25 人天 |
| **合计** | **2 人天** |

---

## 四、资源配置清单

项目开工前与建设过程中需配置的全部资源，分七类列示。其中 **ICP 备案与 AI 企业账号周期最长，须 D1 立即启动**。

### 4.1 软件与中间件

| 软件 | 版本 | 部署方式 |
|---|---|---|
| 操作系统 | Ubuntu 22.04 LTS（时区 UTC） | 服务器预装 |
| Docker Engine + Compose | 最新稳定版 | 主机安装 |
| MySQL | 8.x（库：cp_news、xxl_job；utf8mb4 / utf8mb4_0900_ai_ci） | Docker 容器 |
| RabbitMQ | 4.3.6（exchange：ums、ums.dlx；管理台 15672） | Docker 容器 |
| XXL-JOB 调度中心 | 2.4.0（xxl_job 库 8 张表，首登修改 admin/123456 默认密码） | Docker 容器 |
| nginx | 稳定版（托管前端 dist + /api 反代 8080） | 主机/Docker |
| JDK / Python | JDK 17 / Python 3.11+ | 随服务镜像 |
| Node.js | 20+（仅构建时使用，不入生产镜像） | 构建机 |

### 4.2外部服务与账号

| 资源 | 用途 | 配置方式 |
|---|---|---|
| 智谱 AI（GLM）企业账号 | AI 摘要/翻译/分类，模型 glm-4-flash | 企业注册并开通 API |
| AI API Key | AI 调用凭证 | 仅通过环境变量 `UMS_AI_API_KEY` 注入，不入代码/文档 |
| 短信/公众号（可选） | 推送渠道后续扩展至短信/微信时使用 | 一期可不申请 |

### 4.3 研发协作与数据资源

| 类别 | 资源 |
|---|---|
| 代码托管 | Git 仓库与版本标签管理 |
| 缺陷/任务看板 | 缺陷跟踪工具，P0-P3 分级流转 |
| 测试数据 | 信源黄金样本 HTML；缺日期/纯日期/含 utm/广告/英文等边界样本；管理员/普通/无权限三类账号 |



## 五、需求范围与系统设计

### 5.1 功能范围总览

| 域 | 一期范围 |
|---|---|
| 采集服务 | 信源管理（列表/新增/编辑）、手工触发、XXL-JOB 每日 1 次定时采集、5 试点信源、MQ 发布/降级 |
| AI 加工 | 三段式中文摘要、英文新闻自动翻译、1-3 个分类标签、AI 调用日志、单条/批量补加工 |
| 去重聚合 | 内容指纹、标题归一化、标题相似度三规则归组，跨源文章保留不删 |
| 内容服务 | 时间线（游标分页+组合筛选）、详情、全文搜索、相关内容、事件时间线 |
| 用户体系 | 登录/JWT、RBAC 角色权限、用户管理 |
| 我的情报 | 四 Tab feed（今日重点/重大变化/普通更新/推送记录）、批量已读、关注管理 CRUD |
| 订阅推送 | 多维订阅匹配、推送任务、站内消息、去重与频控、推送启停 |
| 供应链看板 | 原材料/供需库存/贸易物流/供应商动态四区块、外部市场数据显著标注 |
| 管理后台 | 信源、采集任务、内容审核、用户权限、推送管理、审计日志、死信队列 7 页 |

### 5.2 Python 采集服务 ums-crawler-py

代码路径：`services/ums-crawler-py/`

**技术选型**

| 类别 | 技术 | 版本要求 | 用途 |
|---|---|---|---|
| HTTP 抓取 | httpx | ≥0.28 | 异步 HTTP 客户端（支持 HTTP/2） |
| HTML 解析 | parsel | ≥1.11 | CSS/XPath 选择器解析 |
| 限流 / 重试 | aiolimiter / tenacity | ≥1.3 / ≥9.0 | 信源级间隔控制；指数退避重试 |
| 消息队列 | aio-pika | ≥10.0 | 异步发布消息到 RabbitMQ |
| 正文兜底 | trafilatura | ≥2.2 | 选择器失效时正文提取兜底 |
| 日期 / 语言 | dateparser / langdetect | ≥1.4 / ≥1.0.9 | 多格式日期 → UTC；BCP 47 语言识别 |
| JS 渲染 | playwright | ≥1.40 | JS 渲染信源（需 `playwright install chromium`） |
| 触发接口 | FastAPI + uvicorn + pydantic | ≥0.115 / ≥0.30 / ≥2.7 | 内部触发 API |
| 配置格式 | PyYAML | ≥6.0 | 信源选择器独立于代码（selectors/*.yaml） |
| 单元测试 | pytest + pytest-asyncio + pytest-httpx | ≥8.0 / ≥0.24 / ≥0.30 | 黄金样本回归 + 单元测试（目标 ≥20 个） |

**5 个试点信源规划**

| 信源编号 | 信源 | 类型 | 抓取方案 |
|---|---|---|---|
| CN-01 | 中国储能网 | 静态 HTML | httpx + CSS 选择器 |
| CN-02 | 广东水力学会 | 静态 HTML | httpx + CSS 选择器（中文日期解析） |
| INT-01 | Utility Dive | 静态 + WAF | 选择器预配置；WAF 拦截时**不做对抗绕过**，等待人工/浏览器通道决策 |
| INT-02 | Energy-Storage.news | 静态（WordPress） | httpx + h1/time 选择器 |
| INT-03 | eszoneo | JS 渲染 | Playwright，超时放宽至 60s |

**信源接入规范**：新信源接入前必须人工勘察列表/详情/日期选择器，在 YAML 中留勘察注释；"配置意向"不等于"已验证可抓"；选择器放 YAML，新增同类型信源不改核心代码。

### 5.3 Java 主服务 ums-main-java

代码路径：`services/ums-main-java/`

**技术选型**

| 类别 | 技术 | 版本 | 用途 |
|---|---|---|---|
| 基础框架 | Spring Boot | 3.3.5（JDK 17） | 模块化单体 |
| Web / 校验 | spring-boot-starter-web / validation | 随 Boot | REST 接口；参数校验 |
| ORM | MyBatis-Plus | 3.5.7 | 通用 CRUD + 条件构造器 |
| 数据库迁移 | Flyway（flyway-core + flyway-mysql） | 随 Boot | 版本化 SQL，启动自动迁移 |
| 消息队列 | spring-boot-starter-amqp | 随 Boot | RabbitMQ 消费/发布、重试、死信 |
| 监控 / AOP | actuator / starter-aop | 随 Boot | 健康指标；审计日志切面 |
| 调度 | xxl-job-core | 2.4.0 | XXL-JOB 执行器（端口 9999） |
| AI 调用 | JDK 自带 HttpClient | JDK 17 | OpenAI 兼容协议调智谱 GLM |
| 单元测试 | spring-boot-starter-test + spring-rabbit-test | 随 Boot | 目标 ≥40 个 |

**数据模型规划**

业务库 `cp_news`（utf8mb4 / utf8mb4_0900_ai_ci），分三组建表，全部走 Flyway 版本化迁移：

| 分组 | 表 | 用途 |
|---|---|---|
| 采集/内容域 | cp_source | 信源配置（17 字段）+ 5 条种子 |
| | cp_news | 资讯主表；`UNIQUE(source_id, url_hash)` 去重落点；article_id 为 CHAR(36) UUID |
| | cp_news_content | 资讯正文，与主表隔离 |
| | cp_news_analysis | 政策与影响分析 |
| 支撑域 | ai_processing_log | AI 调用日志（provider/model/status/latency/input_digest） |
| | cp_audit_log | 管理操作审计日志 |
| 新增业务域 | users / roles / user_roles | 用户与 RBAC |
| | cp_subscription | 关注/订阅配置 |
| | change_events | 重大变化事件（我的情报数据源） |
| | push_tasks / push_messages | 推送任务与站内消息 |
| | indicator_points | 供应链指标点（可文件/人工导入兜底） |
| | collection_tasks | 采集任务执行记录 |

关键设计：`url_hash` = 规范化 URL 的 SHA-256，绕开 MySQL 768 长度 URL 索引限制；采集字段与 AI/分析字段隔离互不覆盖；更新遵循 `COALESCE(新值,旧值)`，缺失字段不倒退清空，信源名称保留首次快照。

**消息流水线**

```
raw.item.fetched ──► 入库 cp_news + cp_news_content
                         │ 事务提交后
                         ▼
                    content.parsed ──► AI 摘要 + 分类
                         ▼
                    content.processed ──► 指纹计算/回填 → 跨源匹配归组
                         ▼
                    content.finalized ──► 索引/订阅推送
```

| 消息 | 死信队列（DLQ） |
|---|---|
| raw.item.fetched | ums.raw.item.fetched.dlq |
| content.parsed | ums.content.parsed.dlq |
| content.processed | ums.content.processed.dlq |

可靠性设计：本地重试 3 次（1s→2s→4s 指数退避）仍失败进 DLQ，不静默丢失；按 `(source_id, url_hash)` 幂等 upsert；未配 key/正文空记 SKIPPED 正常 ack，AI 故障不阻断入库。

**AI 加工口径**：三段式中文摘要（发生了什么 → 对 BESS 行业意味着什么 → 对 Jinko ESS 的启示）；英文新闻自动翻译为中文；分类从主题字典选 1-3 个标签。

**去重聚合三规则**（仅比对跨源，同源重复由唯一键拦截）：

1. 内容指纹精确相等：`SHA-256(归一化标题 + \n + 归一化正文截断2000)`；
2. 标题归一化相等（NFKC + 小写 + 仅保留汉字/字母/数字）；
3. 标题 3-gram Jaccard ≥ 0.75 且发布日期差 ≤ 7 天（防同名不同事）。

匹配文章共用 `duplicate_group_id`（`dg-`+UUID），各来源文章保留不删。

### 5.4 Web 前端 ums-web

代码路径：`services/ums-web/`

**技术选型**

| 类别 | 技术 | 版本 | 用途 |
|---|---|---|---|
| 框架 / 语言 | React / TypeScript | 18.3 / ~5.6 | 组件模型；类型安全 |
| 构建 | Vite | 6.0 | dev server + 生产构建 |
| UI | Ant Design + @ant-design/icons | 5.22 / 5.5 | 表格/表单/后台组件 |
| 服务端状态 | TanStack Query | 5.62 | 缓存/重试/分页 |
| 路由 | react-router-dom | 6.28 | 三入口嵌套路由 + 管理端懒加载 |
| HTTP / 图表 | axios / echarts-for-react | 1.7 / 5.5 | 统一实例（baseURL `/api`）；看板图表 |
| Mock | MSW | 2.6 | 契约先行、前后端并行开发；接口就绪后移除对应 handler |
| 单元测试 | Vitest + Testing Library + jsdom | 3.2 / 16.1 / 25.0 | 组件/Hook 测试 |

**路由规划**

用户端（AppLayout 顶导）：

| 路径 | 页面 | 说明 |
|---|---|---|
| `/` | → 重定向 `/feed` | — |
| `/feed` | 我的情报 | 今日重点/重大变化/普通更新/推送记录 四 Tab |
| `/feed/subscriptions` | 关注管理 | 内容域/主题/企业/关键词/指标/事件 + 频率渠道 |
| `/center` | 情报中心 | 组合筛选 + 全文搜索 |
| `/center/:contentId` | 资讯详情 | AI 摘要/正文/相关内容/事件时间线/原文入口 |
| `/supply-chain` | 供应链看板 | 原材料/供需库存/贸易物流/供应商动态 |
| `/403` | 无权限页 | — |
| `/admin/*` | 管理后台（懒加载独立 chunk） | 路由守卫 + Suspense |

管理端（AdminLayout 侧栏）：

| 路径 | 页面 |
|---|---|
| /admin/sources | 信源管理 |
| /admin/tasks | 采集任务 |
| /admin/content-review | 内容质量处理 |
| /admin/users | 用户与权限 |
| /admin/push | 推送管理 |
| /admin/audit-logs | 审计日志 |
| /admin/dlq | 死信队列 |

**前端关键规范**：dev 代理 `/api → 8080`，生产 nginx 同源反代；用户端 keyset 游标分页（无总数），管理端 offset 分页；筛选/搜索 300ms 防抖；管理页与图表懒加载；AI 摘要统一标注"AI 生成"；外部市场数据显示醒目"外部市场数据 · 来源：xxx"（SC-007）；DLQ 重放/清空、手工触发二次确认；原文 URL 为空时入口禁用并提示；空状态引导至关注管理页；列表使用骨架屏。

### 5.5 接口规划总览

**A. Python 采集服务（8100，内部调用）**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/healthz` | 健康检查 |
| POST | `/internal/trigger/{source_code}` | 触发单信源采集；body 可带 since/limit；404 选择器不存在，422 配置非法 |

**B. 内容查询（8080）**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/news` | 时间线：sourceId/category/language/dateFrom/dateTo/reviewStatus/cursor/limit |
| GET | `/api/news/{articleId}` | 资讯详情（主表+正文+分析） |
| GET | `/api/v1/search?q=` | 全文搜索（MySQL ngram 全文索引） |
| GET | `/api/v1/contents/{id}/related` | 相关内容 |
| GET | `/api/v1/contents/{id}/timeline` | 事件时间线 |

**C. 我的情报与订阅（需登录）**

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/me/feed?tab=` | 四 Tab feed |
| POST | `/api/me/read-status/batch` | 批量已读 |
| GET/POST/PUT/DELETE | `/api/me/subscriptions[/{id}]` | 关注 CRUD |

**D. 管理后台 `/api/v1/admin`（需管理员权限）**

| 分组 | 方法与路径 |
|---|---|
| 信源 | GET `/sources`、POST `/sources`（新增）、PUT `/sources/{id}`（编辑）、POST `/sources/{id}/trigger?limit=n` |
| 审核 | GET `/reviews`、POST `/reviews/{id}/approve`、`/reject`、PUT `/reviews/{id}/category` |
| 任务 | GET `/tasks`（采集任务记录） |
| 用户 | GET/POST/PUT/DELETE `/users[/{id}]` |
| 推送 | GET `/push`、POST `/push/{id}/toggle` |
| DLQ | GET `/dlq`、GET `/dlq/{queue}/messages`、POST `/dlq/{queue}/requeue`、DELETE `/dlq/{queue}` |
| 审计/调度 | GET `/audit-logs`、GET `/scheduler/status` |

**E. 供应链看板（需登录）**

| 方法 | 路径 |
|---|---|
| GET | `/api/v1/supply-chain/materials` |
| GET | `/api/v1/supply-chain/inventory` |
| GET | `/api/v1/supply-chain/trade-logistics` |
| GET | `/api/v1/supply-chain/suppliers` |

**F. 内部接口**：`POST /internal/ai/reprocess?articleId=` 或 `pendingOnly=true`（AI 补加工）。

> 契约收口要求：`/api/news` 与 `/api/v1/contents` 两套口径需在搜索/相关/时间线开发时收敛为一套契约（分页结构、参数名统一），前端同步切换。

### 5.6 关键 MQ 契约：`raw.item.fetched`（schema 1.0）

Exchange `ums`（durable topic），routing key `raw.item.fetched`：

```json
{
  "schema_version": "1.0",
  "event": "raw.item.fetched",
  "task_id": "同一批次所有条目共享的任务ID",
  "fetched_at": "2026-09-18T01:00:00Z",
  "item": {
    "source_code": "CN-01",
    "title": "文章原标题（保留原文语言）",
    "body": "正文纯文本（去导航广告，保留段落/数字/单位）",
    "author": "署名作者，可空",
    "publish_time": "2026-09-16T08:00:00Z",
    "original_url": "https://...（已去除 utm_* 参数）",
    "language": "zh-CN"
  }
}
```

字段口径：`publish_time` 缺失保留 `null`（禁止用抓取时间替代）；纯日期按 UTC 午夜；时间统一 UTC ISO 8601；`original_url` 去除 `utm_*` 跟踪参数。

---

## 六、总体排期与人天

### 6.1 开发工作量总表（35 人天）

| 阶段 | 工作内容 | 负责人 | 工期 |
|---|---|---|---|
| 1 | **服务器搭建**（明细见第三节） | 开发A | 2 人天 |
| 2 | **Python 采集服务**：连接器抽象、5 信源勘察配置、日期/URL/正文解析、MQ 发布/降级、难点信源联调、≥20 单测 | 开发A | 4 人天 |
| 3 | **Java 核心服务**：MQ 消费入库/幂等/DLQ、AI 加工、去重聚合、内容查询、管理后台核心四模块（信源/审核/DLQ/审计）、XXL-JOB 执行器 | 开发A | 8.5 人天 |
| 4 | **Web 前端**：工程化与布局、我的情报+关注、情报中心+详情、供应链看板、管理后台 7 页 | 开发B | 7.5 人天 |
| 5 | **用户体系 + 我的情报后端 + 订阅推送链路** | 开发A | 6.5 人天 |
| 6 | **搜索与契约收口 + 供应链看板后端 + 采集任务记录/信源新增** | 开发A | 4 人天 |
| 7 | **开发自测与端到端联调**、缺陷修复 | A 主、B 配合 | 2.5 人天 |
| | **开发合计** | | **35 人天** |

阶段 5 明细：用户体系（JWT + RBAC + users/roles + 登录）2 人天；我的情报后端（/me/feed + 已读 + change_events）1.5 人天；订阅推送链路（匹配 → push_tasks → 站内消息 + 去重频控）3 人天。

阶段 6 明细：搜索 + 契约收口（ngram 全文索引 + related/timeline）1 人天；指标对象 + 看板后端（indicator_points + series，可文件/人工导入兜底）2 人天；collection_tasks + 信源 POST 新增 1 人天。

### 6.2 项目总人天汇总

| 角色口径 | 人天 |
|---|---|
| 开发（A 27.5 + B 7.5） | 35 |
| 测试工程师 | 5 |
| 产品经理（需求 1 + UAT 1） | 2 |
| **项目合计** | **42 人天** |

### 6.3 双开发并行排期（工作日）

| 工作日 | 开发A（后端/采集） | 开发B（前端） | QA | PM |
|---|---|---|---|---|
| D1-D2 | 服务器搭建 | 工程初始化 + 设计系统 + 路由布局 | — | 需求确认/原型冻结 |
| D3-D6 | 采集服务 + MQ 契约发布 | 我的情报 + 关注管理（mock 先行） | 测试计划启动 | 信源范围确认 |
| D7-D15 | Java 核心服务（8.5d） | 情报中心/详情/供应链/管理后台页（6.5d） | 用例设计与评审（穿插） | 需求澄清 |
| D16-D18 | 用户体系 | 切真实接口、联调 | 接口测试随交付 | — |
| D19-D20 | 我的情报后端 | 切真实接口、缺陷修复 | 接口/MQ 链路测试 | — |
| D21-D23 | 订阅推送链路 | 缺陷修复/回归配合 | 跟进缺陷验证 | 推送口径确认 |
| D24 | 搜索 + 契约收口 | 契约切换 | — | — |
| D25-D26 | 供应链后端 + 任务/信源新增 | 看板联调 | 系统测试准备 | — |
| D27-D29 | 端到端联调 + 缺陷修复 | 联调 + 缺陷修复 | 系统/兼容性测试 + 回归 | — |
| D30 | 缺陷收尾 | 缺陷收尾 | 验收回归 | **UAT（AC 逐条）** |
| D31 | 上线支持 | 上线支持 | 上线冒烟 | **验收签字/交付** |

**关键里程碑**

| 里程碑 | 时间 | 标志 |
|---|---|---|
| M1 环境就绪 | D2 | 中间件与三服务可启动 |
| M2 契约发布 | D6 | MQ schema 1.0 + 接口契约冻结 |
| M3 核心贯通 | D15 | 采集→加工→查询核心链路通；前端全页面就位 |
| M4 提测（feature freeze） | D26 | 全部功能开发完成，开始系统测试 |
| M5 测试完成 | D29 | 准出标准达标 |
| M6 验收上线 | D31 | UAT 通过并签字 |

### 6.4 关键依赖与并行小项

- 用户体系是我的情报/订阅/管理用户功能的前置，阶段 5 内最先启动；
- 前端以契约 + mock 并行推进，不等待后端；契约变更走评审并通知全员；
- 可并行穿插小项：AI 分类字典外标签归一化（prompt 约束/后处理映射）、枚举补全（crawl_status / policy_status）、ICP 备案（周期最长，**D1 立即启动**）。

---

## 七、测试计划

### 7.1 测试策略

- **左移**：QA 在 D3 介入，参与需求与契约评审，提前输出用例；
- **分层**：单元测试由开发负责（已含在开发工时内，Python ≥20 / Java ≥40 / 前端组件与 Hook 测试），QA 负责接口、集成、系统、兼容性与回归测试；
- **持续**：随开发交付滚动接口测试，缺陷当日登记、当日响应 P0/P1。

### 7.2 测试人员详细职责

测试工程师对项目的**质量过程与质量结论**负责，独立于开发给出测试结论，按项目阶段承担以下职责：

**① 需求与设计阶段（D1-D6）**

- 参加需求评审与原型评审，从可测试性角度提出疑问，识别需求歧义、遗漏与矛盾点；
- 参加接口契约与 MQ 消息契约评审，核对字段口径（publish_time/UTC/URL 去 utm、错误码、鉴权要求）；
- 编写《测试计划》：测试范围、策略、环境、数据、排期、准出标准、风险与应对；
- 依据 PRD/契约设计测试用例，覆盖正常流、边界值、异常流、权限、错误码五类场景，组织用例评审并维护用例基线。

**② 测试准备阶段**

- 申请并搭建独立测试环境，核对与生产同构（版本、配置、时区 UTC、中间件）；
- 准备测试数据：5 信源黄金样本 HTML、缺日期/纯日期/含 utm 参数/广告链接/英文内容等边界样本，管理员、普通用户、无权限用户三类测试账号；
- 准备并校验接口测试集合（按 5.5 接口规划逐接口覆盖）与缺陷看板（字段、分级规则、流转状态）。

**③ 测试执行阶段（滚动随交付）**

- **接口测试**：逐接口验证正常/边界/异常/鉴权/错误码，核对出入参与契约一致性；
- **集成测试**：验证"采集触发 → MQ → 入库 → AI → 归组 → 推送"全链路，含重试 3 次进 DLQ、DLQ 重放、幂等重抓不产生重复数据、AI 故障不阻断入库；
- **系统测试**：用户端三入口与管理后台 7 页全功能验证，覆盖 RBAC 权限矩阵、游标/offset 分页、组合筛选、全文搜索、推送频控、外部数据标注；
- **兼容性测试**：Chrome / Edge / Safari 最新两个大版本、主流分辨率与响应式布局；
- 每条用例记录执行结果；发现缺陷当日登记，写明复现步骤、预期/实际结果、环境版本、日志截图并定级。

**④ 缺陷跟踪与回归阶段**

- 跟踪缺陷全生命周期（新建 → 处理中 → 已修复 → 验证 → 关闭/重开），每日站会同步缺陷状态与阻塞；
- 对已修复缺陷当日复测关闭，回归不通过的重开并通知开发；
- 在里程碑节点执行回归：P0/P1 用例全量回归，受影响模块关联回归；
- 对 P2/P3 遗留缺陷给出风险说明，提交 PM 裁定是否带离上线。

**⑤ 验收与上线阶段**

- 为 UAT 提供环境、数据与操作支持，配合 PM 复现 AC 场景并记录结果；
- 汇总输出《测试报告》：用例执行统计、缺陷统计与遗留项、准出标准逐项核对、测试结论；
- 执行上线前冒烟与上线后冒烟验证，确认核心链路可用后结束测试活动。

**测试交付物**：测试计划、测试用例（含评审记录）、接口测试集合、缺陷清单与统计、测试报告。

**测试人员权限**：有权依据准出标准对未达标版本**拒绝提测/拒绝上线**；缺陷定级有争议时，P0/P2 由 PM 最终裁定，P0 争议未裁定前不得上线。

### 7.3 测试范围与阶段

| 阶段 | 内容 | 执行方 |
|---|---|---|
| 单元测试 | 函数/类/组件级，黄金样本回归 | 开发 |
| 接口测试 | 全部 REST 接口：正常/边界/异常/鉴权/错误码 | QA |
| 集成测试 | 采集触发 → MQ → 入库 → AI → 归组 → 推送全链路；DLQ 重试/重放；幂等重抓 | QA |
| 系统测试 | 用户端三入口 + 管理后台 7 页全功能、权限矩阵、分页/筛选/搜索、频控 | QA |
| 兼容性测试 | Chrome / Edge / Safari 最新两个大版本；主流分辨率与响应式 | QA |
| 回归测试 | 全量 P0/P1 用例 + 缺陷复测 | QA |
| UAT | 按 AC-001~012 业务场景验收 | PM（QA 支持） |

### 7.4 测试环境与数据

- 独立测试环境一套（与生产同构，可用 2C4G 降配）；
- 准备 5 信源黄金样本 HTML、正常/缺日期/非 UTC/含 utm 参数/广告链接等边界样本；
- 测试账号覆盖管理员、普通用户、无权限用户三类角色。

### 7.5 缺陷分级与准出标准

| 级别 | 定义 | 响应要求 |
|---|---|---|
| P0 | 主流程阻断、数据丢失、安全漏洞 | 当日修复 |
| P1 | 核心功能错误、无规避手段 | 24h 内修复 |
| P2 | 功能缺陷但有规避 | 上线前评估修复 |
| P3 | 体验/文案类 | 排期处理 |

**提测准入**：功能开发完成、开发自测通过、单元测试全部通过、接口可访问。
**上线准出**：P0/P1 缺陷为 0；P2 ≤ 3 个且均有规避方案并经 PM 确认；P0/P1 用例回归通过率 100%，用例整体通过率 ≥ 95%；UAT 通过。

### 7.6 测试工时（5 人天）

| 工作项 | 工期 |
|---|---|
| 测试计划 + 用例设计与评审 | 1 人天 |
| 接口测试 + MQ 全链路集成测试 | 1 人天 |
| 系统功能 + 兼容性测试 | 2 人天 |
| 回归 + 缺陷复测 + UAT 支持 | 1 人天 |
| **合计** | **5 人天** |

---

## 八、产品验收

### 8.1 验收组织与角色

| 角色 | 在验收中的职责 |
|---|---|
| 产品经理（验收负责人） | 主持验收；按 AC 逐条执行或确认业务结果；缺陷业务定级与遗留裁定；出具验收结论并签字 |
| 测试工程师（QA） | 准备 UAT 环境与数据；提供操作支持；记录验收结果；不通过项转缺陷并跟踪修复回归 |
| 开发 A / B | 现场支持，快速定位问题；修复验收缺陷并提测 |
| 项目负责人 | 协调资源、确认排期、审批上线 |

验收对象为冻结版本（RC 版本），在 **UAT 环境**进行；验收期间代码冻结，仅允许修复验收缺陷，修复须经 QA 回归后方可更新 UAT 版本。

### 8.2 验收流程

```
① 验收准入检查 → ② 验收准备 → ③ 冒烟通过性检查 → ④ 正式 UAT（逐条执行 AC）
                                              │
                          ┌───────────────────┴───────────────────┐
                          ▼                                       ▼
                    AC 通过，记录结果                      AC 不通过，登记缺陷
                                                                   │
                                          ⑤ 开发修复 → QA 回归 → PM 复验
                                                                   │
                          ⑥ 全部 AC 通过 + 准出标准核对 → ⑦ 验收评审与签字 → ⑧ 上线
```

**① 验收准入检查**：QA 核对准入条件——RC 版本部署完成、系统测试已完成并满足 7.5 上线准出（P0/P1 为 0、用例通过率 ≥95%）、测试报告已输出。不满足则退回，不启动 UAT。

**② 验收准备**：QA 预置验收数据（5 信源内容、三类角色账号、订阅与指标样本）；PM 熟悉 AC 场景与操作路径；约定验收时间窗口与参与人。

**③ 冒烟通过性检查**：正式验收前执行 10-15 分钟冒烟（登录、时间线、手工触发、一条全链路），不通过直接终止本轮验收并退回修复，避免无效 UAT。

**④ 正式 UAT**：PM 按 AC-001~012 顺序逐条执行业务场景，QA 同步记录"通过 / 不通过 / 阻塞"及证据（截图、数据、日志）。

**⑤ 缺陷处理闭环**：不通过项当日登记缺陷并定级——P0/P1 立即修复，修复后 QA 回归、PM 对原 AC 复验；P2/P3 由 PM 裁定当场修复或带离（带离项写入验收报告遗留清单，明确责任人与期限）。每轮缺陷修复后对受影响功能做关联回归。

**⑥ 验收结论汇总**：全部 AC 逐条有结论；QA 汇总 AC 通过情况、缺陷与遗留项，再次核对准出标准。

**⑦ 验收评审与签字**：召开验收评审，PM 出具《产品验收报告》并签字。结论分三档：

- **验收通过**：全部 AC 通过且满足准出标准，批准上线；
- **附条件通过**：无 P0/P1，P2/P3 遗留均有规避方案并明确期限，PM 批准带离上线；
- **不通过**：存在 P0/P1 或核心 AC 未通过，退回整改后重新组织验收（重走 ④-⑦）。

**⑧ 上线**：验收通过后执行部署，QA 完成上线冒烟，PM 确认线上业务正常，项目交付完成。

**验收时限**：UAT 计划 1 个工作日；出现 P0/P1 缺陷时可顺延，但每轮修复-回归-复验不超过 1 个工作日，超过 2 轮由项目负责人重新评估发布计划。

### 8.3 验收标准清单（AC-001~012）

| 编号 | 验收项 | 通过标准 |
|---|---|---|
| AC-001 | 信源管理与手工触发 | 信源列表/新增/编辑可用；手工触发返回抓取汇总，停用信源拒绝触发（409），爬虫不可达返回 502 |
| AC-002 | 定时采集 | XXL-JOB 按每日 1 次自动执行，调度中心可查执行记录 |
| AC-003 | 消息契约与降级 | 消息符合 schema 1.0 全字段口径；publish_time 缺失为 null；URL 无 utm 参数；MQ 不可用时降级 stdout |
| AC-004 | AI 加工 | 三段式中文摘要；英文新闻自动翻译；分类 1-3 个标签；AI 故障不阻断入库，调用有日志可追溯 |
| AC-005 | 情报中心与详情 | 组合筛选、游标分页、搜索、详情、相关内容、事件时间线均可用；原文入口异常时禁用并提示 |
| AC-006 | 订阅管理 | 多维订阅可增删改查；频率/渠道可配置；重复订阅被拦截 |
| AC-007 | 推送链路 | 订阅匹配准确，站内消息可达，重复/超频推送被频控拦截，推送任务可启停 |
| AC-008 | 供应链看板 | 四区块指标正确展示；外部市场数据均带醒目来源标注 |
| AC-009 | 用户体系 | 登录签发 JWT；按 RBAC 控制访问；未登录返回 401，越权返回 403；用户管理可用 |
| AC-010 | 我的情报 | 四 Tab feed 数据正确；批量已读生效；无关注时空状态引导至关注管理 |
| AC-011 | 管理后台 | 审核状态机正确（驳回必须填原因、终态跨向操作拒绝）；DLQ 查看/重放/清空可用；审计日志完整；任务记录可查 |
| AC-012 | 部署与安全基线 | nginx 反代与 SSL 生效；时间线默认仅展示 Approved；管理 token 已配置；非公开端口不对公网暴露 |

### 8.4 交付物清单

- 源代码与版本标签（采集服务、主服务、前端）；
- Flyway 迁移脚本与 5 条信源种子；
- 部署脚本/Compose 文件与部署文档；
- 测试用例、测试报告（含缺陷清单与遗留项）；
- 产品验收报告（含 AC 逐条结论、遗留项清单，PM 签字）。

### 8.5 产品工时（2 人天）

| 工作项 | 工期 |
|---|---|
| 需求澄清、PRD/原型与验收标准确认 | 1 人天 |
| UAT 逐条验收、缺陷优先级裁定、验收报告 | 1 人天 |
| **合计** | **2 人天** |

---

## 九、附录

### 9.1 启动命令速查

```bash
# 基础设施（MySQL / RabbitMQ / XXL-JOB）
docker compose up -d

# Python 采集服务（8100）
RABBITMQ_URL='amqp://<user>:<password>@127.0.0.1:5672/%2F' \
  python services/ums-crawler-py/main.py

# Java 主服务（8080，JDK17）
JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home \
  mvn -f services/ums-main-java/pom.xml spring-boot:run

# Web 前端 dev（5173）/ 生产构建
cd services/ums-web && npm run dev && npm run build
```

### 9.2 测试命令

```bash
# Python 单元测试
cd services/ums-crawler-py && python -m pytest -q

# Java 单元测试
cd services/ums-main-java && \
  JAVA_HOME=/opt/homebrew/opt/openjdk@17/libexec/openjdk.jdk/Contents/Home mvn test

# Web 单元测试
cd services/ums-web && npm test
```

### 9.3 RabbitMQ 初始化约定

- durable topic exchange `ums`；死信 exchange `ums.dlx`；
- 队列由 Java 服务启动自动声明；无消费者绑定的 topic 消息直接丢弃；
- 生产环境禁用 guest 账号，使用最小权限专用账号/vhost。

### 9.4 环境变量清单

**Python（ums-crawler-py）**

| 变量 | 默认值 | 说明 |
|---|---|---|
| RABBITMQ_URL | 空 | MQ 连接串；空则 stdout 降级 |
| MQ_EXCHANGE / MQ_ROUTING_KEY | `ums` / `raw.item.fetched` | 目标 exchange / routing key |
| SELECTORS_DIR | `./selectors` | 选择器目录 |
| CRAWLER_HOST / CRAWLER_PORT | `0.0.0.0` / `8100` | 监听地址 |
| REQUEST_TIMEOUT_SEC / DEFAULT_REQUEST_INTERVAL_SEC | `20` / `2` | 超时 / 请求间隔 |

**Java（ums-main-java）**

| 变量 | 说明 |
|---|---|
| UMS_AI_API_KEY | 智谱 key，为空则 AI 走 SKIPPED；仅通过环境变量注入 |
| UMS_AI_BASE_URL / UMS_AI_MODEL / UMS_AI_ENABLED | AI 地址 / 模型（默认 glm-4-flash）/ 开关 |
| UMS_ADMIN_TOKEN | 管理接口 token，空为本地开放模式，上线前必须配置或切换 Spring Security |
| UMS_XXL_JOB_ENABLED | XXL-JOB 执行器开关 |
| 数据源/MQ 连接 | application.yml 以 `${ENV:默认值}` 占位，生产由环境覆盖 |

**Web（ums-web）**：无必填环境变量；baseURL 固定 `/api`。

### 9.5 错误码分段

| 前缀 | 模块 | 示例 |
|---|---|---|
| AUTH | 认证权限 | AUTH_4011 未登录；AUTH_4031 无权限 |
| SRC | 信源管理 | SRC_4041 不存在；SRC_4091 编码重复；SRC_4221 参数不合法 |
| CNT | 内容服务 | CNT_4041 不存在；CNT_4221 筛选非法 |
| IND | 指标服务 | IND_4041 编码不存在 |
| SUB | 订阅推送 | SUB_4041 不存在；SUB_4091 重复订阅 |
| AI | AI 适配层 | AI_5031 服务不可用（不阻断入库） |
| SYS | 通用系统 | SYS_5001 内部错误；SYS_4291 限流 |

### 9.6 上线前检查清单

- [ ] 配置 `UMS_ADMIN_TOKEN` 或切换 Spring Security + RBAC；
- [ ] 时间线默认展示口径改为 Approved；
- [ ] MQ 生产专用 vhost、密码入配置中心、每日快照备份；
- [ ] nginx 托管 dist + `/api` 反代 8080 + SSL 证书；
- [ ] CORS 白名单按生产域名收紧；
- [ ] XXL-JOB 每日任务在调度中心启用；
- [ ] ICP 备案完成；AI API 企业账号就绪；
- [ ] 全链路无 P0/P1 遗留缺陷。

---

> 本文档为项目整体规划基线；如需求、契约或排期变更，须经评审后更新版本号，并同步通知全体角色。
