# 储能资讯平台 · 管理员初始化指南 v1.0

> 适用对象：负责首次部署/初始化本平台的管理员
> 目标：从一台全新服务器开始，完成环境搭建 → 服务启动 → 数据初始化 → 管理员登录 → 冒烟验证的全流程
> 配套文档：《储能资讯平台部署运维文档_v1.0.md》（日常运维/故障排查/升级回滚）、《储能资讯平台接口文档》（API 契约）

---

## 1. 项目组成与初始化总览

| 组成 | 目录 | 端口 | 作用 |
|---|---|---|---|
| Java 主服务 | `services/ums-main-java` | 8080 | 核心 API、MQ 消费、AI 加工、用户体系、管理后台接口 |
| React 前端 | `services/ums-web` | 5173（dev） | 三入口 UI（资讯 Feed / 供应链看板 / 管理后台） |
| Python 采集服务 | `services/ums-crawler-py` | 8100 | 多信源采集，产出 `raw.item.fetched` 消息 |
| MySQL 8 | 中间件 | 3306 | 业务库 `cp_news`（Flyway 自动建表） |
| RabbitMQ | 中间件 | 5672 / 15672 | 事件总线（exchange `ums`）+ 管理台 |

**初始化总流程：**

```
① 安装基础软件 → ② 启动中间件（MySQL/RabbitMQ）→ ③ 构建并启动 Java 主服务
（Flyway 自动建表 + 种子数据）→ ④ 启动前端 → ⑤ admin 登录冒烟验证
→ ⑥（可选）启动 Python 采集服务 → ⑦ 上线前安全加固
```

---

## 2. 前置条件

### 2.1 硬件建议

- 4C8G / 100G SSD / Ubuntu 22.04（详见运维文档第一章）

### 2.2 软件版本要求

| 软件 | 版本 | 验证命令 |
|---|---|---|
| JDK | 17+ | `java -version` |
| Maven | 3.8+ | `mvn -v` |
| Node.js | 18+ | `node -v` |
| npm | 9+ | `npm -v` |
| Python | 3.10+ | `python3 --version` |
| MySQL | 8.0+ | `mysql --version` |
| RabbitMQ | 3.x（含管理插件） | `rabbitmqctl status` |

### 2.3 获取代码

```bash
git clone <项目仓库地址> ConsultationPlatform
cd ConsultationPlatform
```

---

## 3. 中间件初始化

### 3.1 MySQL

```sql
-- root 登录后执行
CREATE DATABASE IF NOT EXISTS cp_news
  DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
CREATE USER 'ums_app'@'%' IDENTIFIED BY '<强密码>';
GRANT ALL PRIVILEGES ON cp_news.* TO 'ums_app'@'%';
FLUSH PRIVILEGES;
```

> 说明：库表结构由 Java 服务启动时 Flyway 自动迁移（V1–V6），**无需手工导 SQL**。

### 3.2 RabbitMQ

```bash
# 创建业务用户与 vhost（默认 /）
rabbitmqctl add_user ums_admin <强密码>
rabbitmqctl set_permissions -p / ums_admin ".*" ".*" ".*"
# 确保管理插件已启用（管理台 15672）
rabbitmq-plugins enable rabbitmq_management
```

验证：浏览器打开 `http://127.0.0.1:15672`，用 `ums_admin` 能登录即成功。

> 交换机 `ums`（topic）、各队列与 DLQ 由 Java 服务启动时自动声明。

---

## 4. Java 主服务初始化

### 4.1 构建

```bash
cd services/ums-main-java
mvn clean package -DskipTests
# 产物：target/ums-main-0.1.0-SNAPSHOT.jar
```

### 4.2 配置环境变量

后端所有配置通过环境变量注入（占位符写在 `src/main/resources/application.yml`，默认值仅适合本机开发）。在 `services/ums-main-java/` 下创建 `.env` 文件（已存在则修改），启动前 `source`：

```bash
# ---------- 必填：中间件 ----------
export UMS_MYSQL_HOST=127.0.0.1
export UMS_MYSQL_PORT=3306
export UMS_MYSQL_DB=cp_news
export UMS_MYSQL_USER=ums_app
export UMS_MYSQL_PASSWORD=<MySQL密码>

export UMS_MQ_HOST=127.0.0.1
export UMS_MQ_PORT=5672
export UMS_MQ_USER=ums_admin
export UMS_MQ_PASSWORD=<MQ密码>

# ---------- 必填：安全 ----------
export UMS_JWT_SECRET=<32字节以上随机串，生产必须更换>
export UMS_JWT_TTL_MINUTES=480

# ---------- 必填：认证模式 ----------
# LOCAL = 本地开发模式（见 4.4 安全警告）；公司部署用 IAM
export UMS_AUTH_PROVIDER=LOCAL
export UMS_IAM_ENABLED=false

# ---------- 可选：AI 加工 ----------
export UMS_AI_ENABLED=true
export UMS_AI_API_KEY=<智谱API Key>     # 只走环境变量，不得写入代码/文档
export UMS_AI_BASE_URL=https://open.bigmodel.cn/api/paas/v4
export UMS_AI_MODEL=glm-4-flash

# ---------- 可选：RabbitMQ 管理台（DLQ 重放功能用） ----------
export UMS_MGMT_URL=http://127.0.0.1:15672
export UMS_MGMT_USER=ums_admin
export UMS_MGMT_PASSWORD=<MQ密码>

# ---------- 可选：采集服务地址（触发抓取用） ----------
export UMS_CRAWLER_URL=http://127.0.0.1:8100
```

IAM 模式（公司接入时）完整变量见 `.env.iam.example`：将 `UMS_AUTH_PROVIDER=IAM`、`UMS_IAM_ENABLED=true`，并填入 `UMS_IAM_AUTH_API_URL`（公司 IAM 认证 API 地址）、`UMS_IAM_AUTH_MODE`（API_KEY/BEARER/NONE）及对应凭据。首次 IAM 登录会自动创建 `cp_user`（`UMS_IAM_AUTO_PROVISION=true`）。

### 4.3 启动

```bash
cd services/ums-main-java
source .env
java -jar target/ums-main-0.1.0-SNAPSHOT.jar
```

常用启动参数：

- **RabbitMQ 尚未就绪/暂不消费**（如纯本地调试）：
  `java -jar target/ums-main-0.1.0-SNAPSHOT.jar --spring.rabbitmq.listener.simple.auto-startup=false`
- 生产建议用 systemd / nohup 托管（见运维文档第六章），**不要在 SSH 会话前台裸跑**（会话结束进程被杀）。

验证启动成功：

```bash
curl http://127.0.0.1:8080/actuator/health
```

### 4.4 首次启动自动完成（无需手工操作）

| 迁移 | 内容 |
|---|---|
| V1 | 资讯主表 `cp_news`、正文表、信源表及**种子信源** |
| V2–V4 | 采集任务、审核流等 |
| V5 | 用户/订阅/推送五表 + **种子 admin 用户**（LOCAL 模式，角色 `USER,ADMIN`） |
| V6 | 供应链看板六表 + **指标/库存/贸易/供应商种子数据** |

> 残留提示：V5 中含测试信源 `TEST-01`，上线前请在管理后台「信源管理」中停用或忽略。

### 4.5 认证模式安全说明（重要）

- **LOCAL 模式（默认）**：仅校验用户名存在且状态 ACTIVE，**不校验密码**，仅限内网开发/演示使用。生产环境严禁开启。
- **IAM 模式**：用户名+密码提交至公司 IAM 认证 API 校验（`UMS_IAM_AUTH_API_URL`），为生产推荐方式。
- 切换方式：修改 `UMS_AUTH_PROVIDER` 与 `UMS_IAM_ENABLED` 后重启后端；前端将 `.env` 中 `VITE_AUTH_PROVIDER` 同步设为 `LOCAL` 或 `IAM`（控制登录框样式：密码框是否显示）。

---

## 5. 前端初始化

```bash
cd services/ums-web
npm install

# 开发模式（端口 5173）
npm run dev

# 生产构建（产物 dist/，交由 nginx 托管，见运维文档）
npm run build
```

前端环境变量（`services/ums-web/.env`）：

```
VITE_AUTH_PROVIDER=LOCAL   # LOCAL | IAM，与后端模式保持一致
```

---

## 6. Python 采集服务初始化（可选）

```bash
cd services/ums-crawler-py
python3 -m venv .venv && source .venv/bin/activate
pip install -r requirements.txt
# 启动（:8100）
python -m uvicorn api.app:app --host 0.0.0.0 --port 8100
```

验证与触发：

```bash
curl http://127.0.0.1:8100/healthz
curl -X POST http://127.0.0.1:8100/internal/trigger/<source_code>
```

说明：
- 信源选择器配置在 `selectors/*.yaml`，与代码解耦，可按需增改；
- 采集结果以 `raw.item.fetched` 消息发到 RabbitMQ（不直接落库），MQ 不可用时降级输出 stdout 日志；
- WAF 按 TLS 指纹拦截的信源不做对抗绕过，属预期行为；
- 调度可接 XXL-JOB（`UMS_XXL_JOB_*`，默认关闭）。

---

## 7. 管理员首次登录与冒烟验证

### 7.1 登录

1. 浏览器打开前端（dev：`http://127.0.0.1:5173`；生产为 nginx 域名）；
2. 使用种子账号登录：用户名 **`admin`**（LOCAL 模式下密码任意填写即可，见 4.5 安全说明；IAM 模式下用公司 IAM 密码）；
3. 登录成功后进入资讯 Feed 首页，右上角显示「系统管理员」。

接口层验证（可选）：

```bash
curl -X POST http://127.0.0.1:8080/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"admin","password":"any"}'
# 返回 JWT token
```

### 7.2 冒烟清单（全部通过即初始化完成）

| # | 检查项 | 操作 | 预期 |
|---|---|---|---|
| 1 | 后端健康 | `curl :8080/actuator/health` | `{"status":"UP"}` |
| 2 | Feed 资讯 | 前端「资讯」页 | 列表加载，无接口报错 |
| 3 | 供应链看板 | 前端「供应链」页 | 六个指标卡片、30 天走势图、库存/贸易/供应商区块（V6 种子数据） |
| 4 | 管理后台-信源 | 「管理后台 → 信源管理」 | 种子信源列表渲染 |
| 5 | 管理后台-任务 | 「采集任务」页 | 各信源调度状态表格 |
| 6 | 管理后台-审计 | 「审计日志」页 | 记录列表、展开 JSON 正常 |
| 7 | 用户管理 | 「用户与权限」 | admin 用户可见，可编辑 |
| 8 | MQ 链路 | RabbitMQ 管理台 → Queues | `ums.*.queue` 系列已声明 |
| 9 | 死信能力 | 管理后台「异常处理」页 | 3 个 DLQ 队列可见（可 purge/requeue） |
| 10 | 采集触发（可选） | 启动 Python 服务后 POST trigger | `cp_collection_task` 出现新记录 |

### 7.3 创建业务账号（按需）

管理后台 → 用户与权限 → 新增用户（设置角色 `USER`/`ADMIN`）；或接入 IAM 后由员工首次登录自动开户（`UMS_IAM_AUTO_PROVISION=true`）。

---

## 8. 常见初始化问题排查

| 现象 | 原因 | 处理 |
|---|---|---|
| 后端启动报 `Communications link failure` | MySQL 未启动/账号密码错 | 核对 `UMS_MYSQL_*`；确认 MySQL 已运行 |
| 后端启动报 `AMQPConnection` 相关错误 | RabbitMQ 未启动 | 先起 MQ；或调试期加 `--spring.rabbitmq.listener.simple.auto-startup=false`（仅跳过监听，连接仍需可达） |
| Flyway 报 `Validate failed` | 库中有手工改动/版本混乱 | 对照 `flyway_schema_history`；新环境建议清库重建 |
| 登录返回 401 | LOCAL 未生效（`.env` 里 `UMS_IAM_ENABLED=true` 且 IAM 地址为占位符） | `export UMS_IAM_ENABLED=false`、`UMS_AUTH_PROVIDER=LOCAL` 后重启 |
| 前端页面空白/接口 404 | 前端未起或后端端口不符 | 确认 5173/8080；检查 vite 代理配置 |
| 登录后接口大量 401/409 异常 | JWT secret 启动后被更换 | 保持同一 `UMS_JWT_SECRET`，让用户重新登录 |
| 供应链页无数据 | V6 未迁移成功 | 查启动日志 Flyway 段；确认 `cp_indicator_series` 等六表有种子行 |
| 采集触发无响应 | Python 服务未启动 | `curl :8100/healthz` 确认后重启 |

更多故障场景（DLQ 堆积、AI 加工失败、内存/磁盘）见运维文档第九章。

---

## 9. 上线前安全加固清单

- [ ] `UMS_JWT_SECRET` 已更换为生产随机值（≥32 字节）
- [ ] 认证切至 IAM：`UMS_AUTH_PROVIDER=IAM`、填入公司 IAM 真实地址与凭据
- [ ] MySQL/MQ 均使用强密码，且仅对应用服务器网段开放
- [ ] 种子测试信源 `TEST-01` 已停用
- [ ] `UMS_AI_API_KEY` 仅存在于服务器环境变量，未入代码/文档/仓库
- [ ] 前端走 nginx HTTPS（部署拓扑见运维文档）
- [ ] 首次登录后按运维文档完成巡检项

---

*文档版本 v1.0 · 依据代码库当前状态（Flyway V1–V6、37 端点、LOCAL/IAM 双认证模式）编写；后续钉钉推送渠道接入后需补充 `UMS_DINGTALK_*` 配置项。*
