# 储能资讯平台操作手册 v1.0

> 面向对象：新用户（普通用户 / 管理员） ｜ 更新日期：2026-09-20

## 1. 系统简介

储能资讯平台（UMS）是一站式储能行业情报系统，提供三大能力：

1. **资讯情报**：自动采集国内外储能行业新闻，AI 生成三段式中文摘要与分类，支持订阅关键词推送
2. **供应链看板**：原材料价格、库存供需、贸易物流、供应商风险四大看板
3. **管理后台**：信源管理、采集监控、内容审核、用户与推送管理（仅管理员）

---

## 2. 快速开始

> 若系统尚未部署（浏览器打不开登录页），请先由管理员参照**附录 A：系统初始化**完成部署，再回到本节。

### 2.1 登录

1. 浏览器打开系统地址（开发环境为 `http://localhost:5173`），自动跳转到登录页
2. 输入**用户名**和**密码**，点击登录
3. 登录成功后进入「资讯时间线」首页

> 管理员账号由系统初始化（本地开发环境：`admin`）。普通用户账号请联系管理员在「管理后台 → 用户管理」中创建，或使用公司 IAM 统一认证登录。

### 2.2 页面导航

登录后左侧为固定导航栏：

| 菜单 | 路径 | 说明 |
|------|------|------|
| 资讯 | `/feed` | 资讯时间线（首页） |
| 订阅 | `/feed/subscriptions` | 我的订阅规则管理 |
| 内容中心 | `/center` | 全部内容浏览与搜索 |
| 供应链 | `/supply-chain` | 供应链看板 |
| 管理后台 | `/admin` | 仅 ADMIN 角色可见 |

---

## 3. 普通用户功能

### 3.1 资讯时间线（首页）

进入「资讯」后，顶部有四个 Tab：

| Tab | 内容 | 使用场景 |
|-----|------|----------|
| **今日** | 最近 24 小时发布的最新资讯 | 每天上班第一眼，快速掌握昨天到现在的动态 |
| **未读** | 你还没有过的资讯 | 配合已读状态，不遗漏任何一条 |
| **订阅** | 命中你订阅规则的资讯 | 只看自己关心的话题（如"钠离子电池"） |
| **热门** | 热度排序的重点资讯 | 了解全平台都在关注什么 |

**操作技巧**：
- 列表下滑自动加载更多（游标分页，不会重复/漏条）
- 点击任意一条进入详情页
- 「未读」Tab 下可使用**批量标记已读** / **全部已读**按钮，快速清理

### 3.2 阅读资讯详情

从时间线或内容中心点击进入 `/center/{内容ID}` 详情页，可以看到：

- **AI 三段式摘要**：事实 → 影响 → 建议，英文新闻自动翻译成中文
- **原文入口**：点击「查看原文」跳转信源原始链接
- **相关内容**：页面下方展示同主题的其他资讯，用于追踪同一事件的各方报道
- **事件时间线**：展示该主题的演化过程（首次检测 → 热度上升 → 重大变化 → 稳定跟踪），适合了解一个事件的来龙去脉

> 字段旁标注「AI」角标的为 AI 生成内容；「外部市场数据 / 内部数据」徽标标识数据口径来源。

### 3.3 搜索与内容中心

进入「内容中心」：

- 顶部**搜索框**输入关键词（中英文均可），回车即搜
- 支持按**来源、分类、语言、日期区间**组合筛选
- 结果与时间线一致，点击进入详情

### 3.4 订阅管理（情报推送）

进入「订阅」页面：

1. 点击**新建订阅**
2. 设置订阅条件（可组合）：
   - **关键词**：如"碳酸锂"、"sodium-ion"
   - **分类**：如 Policy（政策）、BESS（电池储能）
   - **来源**：限定特定信源
3. 保存后，新采集的资讯命中规则时会：
   - 出现在首页「订阅」Tab
   - 生成**站内消息**提醒（右上角消息铃铛）

**管理已有订阅**：列表中可随时编辑条件或删除。建议订阅 3～5 条精准规则，过宽的规则（如只选分类不填关键词）会导致推送泛滥——系统内置频控与去重，但精准订阅体验最佳。

### 3.5 供应链看板

进入「供应链」页面，自上而下四个区块：

| 区块 | 内容 | 操作 |
|------|------|------|
| **原材料市场** | 6 张指标卡：电池级碳酸锂、磷酸铁锂正极、电解液、铜箔、电芯价格、采购成本指数 | **点击卡片**展开近 30 天走势折线图；卡片上直接显示当前值与环比涨跌（红涨绿跌） |
| **供需与库存** | 社会库存、仓单数量（柱状）+ 行业排产（折线）组合图 | 鼠标悬停查看各月数值 |
| **贸易与物流** | 提单记录表（货物、对手方、航线、状态） | 顶部下拉**按状态筛选**（在途/已到港/清关中/已完成）；**点击行**展开物流时间线（装船→离港→到港→清关） |
| **供应商动态** | 供应商风险卡片（扩产/减产/停产/信用风险变化/新品） | 红色边框=高风险需关注；绿色=利好 |

> 每个图表上方有数据口径徽标（如 SMM、广期所、内部采购系统），用于区分外部市场数据与内部数据。

---

## 4. 管理员功能（ADMIN 角色）

> 普通用户访问管理后台会被重定向到「403 无权限」页面。管理员账号在「用户管理」中为用户分配 ADMIN 角色。

### 4.1 信源管理

**查看信源**：进入「管理后台 → 信源管理」，表格列出全部信源（ID、名称、区域、类型、抓取状态、启停、频率、优先级）。

**新增信源**：
1. 点击右上角**「+ 新增信源」**
2. 填写必填项：信源 ID（如 `CN-05`）、信源名称、入口 URL、默认语言
3. 可选项：覆盖区域、来源类型、抓取频率（默认 1 次/天）、优先级（P0 最高）、主题标签（JSON 数组，如 `["BESS","Policy"]`）、备注
4. 提交后信源立即可用，采集器按频率自动抓取

**编辑信源**：行内「编辑」按钮，可修改名称、启停定时抓取、调整频率/优先级/备注。

**手动触发采集**：行内「触发采集」按钮——不等定时计划，立刻抓取一次该信源（单次上限 10 条）。适合新增信源后的首次验证。

### 4.2 采集任务

进入「采集任务」页面：

- **上方调度状态卡**：单次触发上限、当前到期信源数（橙色提醒>0）、启用信源数
- **信源到期表**：每个信源下次调度时间，快到期/已到期的会高亮
- **下方任务执行记录**：每次采集（手动/定时）的历史，包含：
  - 触发方式（手动/定时）、开始结束时间、**耗时**
  - 抓取条数 / 入库条数 / 失败条数
  - 状态徽章：绿色成功 / 橙色部分失败 / 红色失败

**排查采集失败**：先看此页任务记录——若 `fetched=0` 且状态失败，通常是信源页面改版或网络问题，需在信源管理中核对该信源或联系开发调整选择器配置。

### 4.3 内容审核

进入「内容审核」：

- 列表展示待人工复核的资讯（AI 置信度不足或命中敏感规则的）
- **通过**：内容进入正式发布流，对用户可见
- **驳回**：内容不展示
- **修正分类**：AI 分类不准时，可手动改挂正确分类

### 4.4 用户管理

- **用户列表**：查看全部用户（角色、状态、创建时间）
- **新增用户**：创建账号并分配角色（普通用户 / ADMIN）
- **编辑**：调整角色、启用/停用账号
- **删除**：移除用户（操作会记录审计日志）

### 4.5 推送任务

进入「推送任务」：查看订阅推送的执行记录（任务、目标用户、命中条数、发送状态），可用**启停开关**暂停/恢复某个推送任务。

### 4.6 审计日志

进入「审计日志」：所有管理端敏感操作（信源增改、触发采集、审核、用户增删等）的留痕记录。

- 顶部可**按操作类型筛选**（新增信源/信源配置更新/手动触发采集…）
- **点击行展开**：以字段级 diff 表格展示本次操作具体改了什么（字段名、变更前、变更后、变更类型高亮）

### 4.7 死信队列（DLQ）

进入「DLQ」：消息队列处理失败的死信堆积情况。

- **概览**：各队列堆积数量
- **查看明细**：具体失败消息内容
- **重新入队**：修复问题后将死信放回队列重新处理
- **清空**：确认无用后清除

> DLQ 堆积 > 0 时说明采集→加工链路有异常，应先查看明细定位原因，而非直接清空。

---

## 5. 常见问题（FAQ）

**Q1：登录提示 401？**
检查用户名密码；若使用公司 IAM 模式，确认账号为在职状态。连续失败请联系管理员在用户管理中核对账号状态。

**Q2：资讯列表是空的？**
资讯依赖采集链路。管理员可在「采集任务」查看是否有成功记录；若全部失败，检查采集服务（ums-crawler，端口 8100）与消息队列是否运行。

**Q3：订阅了关键词但收不到推送？**
① 确认订阅规则已启用；② 检查首页「订阅」Tab 是否有内容——有内容但无消息提醒，可能是推送任务被停用（管理员在「推送任务」中查看）；③ 新资讯命中规则才会推送，历史存量不补推。

**Q4：搜索搜不到已知的内容？**
搜索基于全文索引，新入库内容可能有分钟级延迟；中文请输入完整词（如"碳酸锂"而非"碳"）。

**Q5：供应链数据多久更新？**
指标时序数据按导入频率更新（当前为种子/人工导入模式）；页面缓存 5 分钟，刷新页面即可获取最新。

**Q6：普通用户误入管理后台？**
会看到 403 无权限页。需要管理权限请联系管理员在「用户管理」中将你的角色改为 ADMIN。

---

## 6. 新用户上手路径（建议）

```
第 1 天：登录 → 首页四 Tab 浏览 → 阅读几条资讯详情（熟悉 AI 摘要格式）
第 2 天：创建 2~3 条订阅规则 → 次日查看「订阅」Tab 命中情况
第 3 天：浏览供应链看板 → 点击指标卡看走势 → 筛选在途物流
日常：   早上看「今日」+「未读」→ 消息铃铛处理订阅推送 → 需要追溯事件时用详情页时间线
```

---

## 附录 A：系统初始化（管理员专用）

> 普通用户可跳过本附录。完整细节（含故障排查、安全加固清单）见**《储能资讯平台管理员初始化指南_v1.0.md》**，本附录仅为核心速查。

### A.1 初始化总流程

```
① 安装基础软件（JDK17/Maven/Node18/Python3.10/MySQL8/RabbitMQ）
→ ② 启动中间件（MySQL 建库 cp_news + RabbitMQ 建用户）
→ ③ 构建并启动 Java 主服务（Flyway 自动建表 V1–V6 + 种子数据）
→ ④ 启动前端
→ ⑤ admin 登录冒烟验证
→ ⑥（可选）启动 Python 采集服务
→ ⑦ 上线前安全加固
```

### A.2 关键命令速查

| 步骤 | 命令 | 说明 |
|------|------|------|
| 构建后端 | `cd services/ums-main-java && mvn clean package -DskipTests` | 产出 `target/ums-main-0.1.0-SNAPSHOT.jar` |
| 配置环境变量 | `source .env` | MySQL/MQ/JWT/IAM/AI 等，模板见初始化指南 4.2 |
| 启动后端 | `java -jar target/ums-main-0.1.0-SNAPSHOT.jar` | MQ 未就绪时加 `--spring.rabbitmq.listener.simple.auto-startup=false` |
| 启动前端 | `cd services/ums-web && npm install && npm run dev` | 生产用 `npm run build` 交 nginx 托管 |
| 启动采集（可选） | `cd services/ums-crawler-py && pip install -r requirements.txt && python -m uvicorn api.app:app --port 8100` | 不启动则无新资讯入库 |
| 健康验证 | `curl http://127.0.0.1:8080/actuator/health` | 返回 `{"status":"UP"}` 即成功 |

### A.3 初始化自动完成的内容（无需手工操作）

- **Flyway 建表**：V1–V6 自动迁移（资讯主表、信源、用户/订阅/推送、供应链看板六表）
- **种子数据**：admin 用户（LOCAL 模式密码任意填写）、种子信源、供应链演示数据
- **MQ 拓扑**：exchange `ums` 与各队列/DLQ 自动声明

### A.4 初始化后管理员必做的三件事

1. **停用测试信源**：管理后台 → 信源管理 → 停用 `TEST-01`（V5 残留测试数据）
2. **创建业务账号**：管理后台 → 用户管理 → 新增用户并分配角色（或接入公司 IAM 后首次登录自动开户）
3. **配置生产认证**：`UMS_AUTH_PROVIDER=IAM` 并填入公司 IAM 真实地址（LOCAL 模式不校验密码，严禁生产使用）；同时更换 `UMS_JWT_SECRET` 为 32 字节以上随机值

### A.5 初始化验收标准

冒烟清单全部通过即初始化完成（10 项详细清单见初始化指南 7.2），核心四项：

- [ ] 后端健康检查返回 UP
- [ ] 前端资讯页/供应链看板正常加载
- [ ] admin 可登录并进入管理后台
- [ ] RabbitMQ 管理台（:15672）可见 `ums.*` 队列已声明

### A.6 常见初始化故障速查

| 现象 | 处理 |
|------|------|
| 后端启动报 MySQL 连接失败 | 确认 MySQL 已运行、`UMS_MYSQL_*` 配置正确 |
| 后端启动报 AMQP 连接错误 | 先启动 RabbitMQ，或调试期加 `auto-startup=false` 参数 |
| 登录返回 401 | `.env` 中 `UMS_IAM_ENABLED=false`、`UMS_AUTH_PROVIDER=LOCAL` 后重启 |
| 供应链页无数据 | 检查启动日志 Flyway V6 是否迁移成功 |
| 采集触发无响应 | `curl :8100/healthz` 确认 Python 采集服务已启动 |
