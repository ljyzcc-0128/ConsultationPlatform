# 储能资讯平台部署运维文档 v1.0

| 项 | 说明 |
|---|---|
| 文档目的 | 指导一期系统在 Linux 单机环境的部署、配置、验证与日常运维 |
| 适用读者 | 运维工程师、后端/前端开发、测试负责人 |
| 关联文档 | 《储能资讯平台一期_三层技术开发文档_v1.0》（架构与契约基线）、《储能资讯平台接口文档_v1.0》（接口清单）、《储能资讯平台操作手册_v1.0》（用户操作） |
| 更新日期 | 2026-09-20 |

---

## 一、系统架构与部署拓扑

### 1.1 组件清单

| 组件 | 技术 | 端口 | 部署方式 | 职责 |
|---|---|---|---|---|
| nginx | 稳定版 | 80 / 443 | 主机安装 | 托管前端 dist、`/api` 反代 Java、SSL 终结 |
| ums-web | React 18 + Vite 6 | —（静态资源） | 构建产物部署至 nginx | 用户三入口 + 管理后台界面 |
| ums-main-java | Spring Boot 3.3 / JDK 17 | 8080 | systemd 常驻 jar | 业务 REST、管理接口、MQ 消费、AI 加工、XXL-JOB 执行器（9999） |
| ums-crawler-py | FastAPI / Python 3.11+ | 8100 | systemd 常驻 | 信源抓取，消息发布 `raw.item.fetched` |
| MySQL | 8.x（utf8mb4） | 3306 | Docker 容器 | 业务库 `cp_news`（Flyway 自动迁移） |
| RabbitMQ | 4.x | 5672 / 15672 | Docker 容器 | exchange `ums` / `ums.dlx`，管理台 15672 |
| XXL-JOB 调度中心 | 2.4.0（可选） | 8081 | Docker 容器 | 采集定时调度（一期可先手动/系统 cron 触发） |

### 1.2 部署拓扑（单机）

```
                 ┌──────────────────────────── 公网（仅放行 22/80/443） ─────────────────────┐
 用户/管理员 ──► │ nginx(80/443)                                             │
                 │   ├── /            → ums-web 静态资源（dist）                 │
                 │   └── /api/*       → 反代 127.0.0.1:8080（Java 主服务）        │
                 │                                                             │
                 │ Java 主服务(8080) ──► MySQL(3306, Docker)                     │
                 │        │            ──► RabbitMQ(5672, Docker)               │
                 │        │            ──► 触发采集 → ums-crawler(8100, 仅内网)   │
                 │        └── XXL-JOB 执行器(9999) ← 调度中心(8081, Docker)      │
                 └─────────────────────────────────────────────────────────────┘
```

- **Python 采集服务（8100）不对公网开放**，仅接收 Java 主服务或 XXL-JOB 的内部触发调用；
- 3306 / 5672 / 8081 / 8100 / 9999 仅限本机（127.0.0.1）或内网访问；
- 一期为**单机部署**（4C8G / 100G SSD / Ubuntu 22.04 LTS，时区 UTC），后续按负载拆分。

### 1.3 数据流（与运维相关部分）

```
XXL-JOB/cron ─► Java(POST /internal/trigger/{code}) ─► crawler(8100 抓取)
   ─► RabbitMQ exchange ums / routing key raw.item.fetched
   ─► Java 消费 → 去重入库 → AI 加工（glm-4-flash）→ 可检索/可订阅推送
   （消费失败重试 3 次 → 死信队列 ums.raw.item.fetched.dlq）
```

- RabbitMQ 不可达时，**Python 采集服务自动降级为 stdout 日志输出**，不中断服务；
- AI 加工失败不影响入库（状态记为 SKIPPED / FAILED，见 `ai_processing_log` 表）。

---

## 二、环境要求与部署前准备

### 2.1 软件版本

| 软件 | 版本要求 | 用途 |
|---|---|---|
| Ubuntu | 22.04 LTS（时区 UTC） | 操作系统 |
| Docker Engine + Compose 插件 | 最新稳定版 | 中间件容器化 |
| JDK | 17 | 运行 Java 主服务 |
| Python | 3.11+（含 venv） | 运行采集服务 |
| Node.js | 20+（**仅构建机使用，不入生产**） | 前端构建 |
| Maven | 3.9+ | Java 打包（构建机） |
| nginx | 稳定版 | 前端托管 + 反代 |

### 2.2 部署前检查项

- [ ] 服务器安全组仅放行 22 / 80 / 443，其余端口仅本机访问；
- [ ] 创建部署用户（如 `ums`）与 SSH key，禁用 root 密码登录；
- [ ] 智谱 AI 企业账号与 API Key 已开通（`UMS_AI_API_KEY` 只通过环境变量注入，不写入代码/文档/仓库）；
- [ ] 公司 IAM 认证 API 地址已确认（接入方式见 8.6 节，未就绪前可用 LOCAL 模式）；
- [ ] ICP 备案完成（使用域名 + 80/443 对外服务的前提）；
- [ ] 生产域名与 SSL 证书（Let's Encrypt 或公司证书）已就绪。

### 2.3 目录规划

```
/opt/ums/
├── app/                      # Java jar 与 Python 服务
│   ├── ums-main-0.1.0-SNAPSHOT.jar
│   └── ums-crawler-py/       # Python 代码目录（含 selectors/）
├── web/dist/                 # 前端构建产物（nginx root 指向此处）
├── configs/                  # docker-compose.yml、.env、nginx 配置
└── logs/                     # 应用日志（/var/log/ums 软链或直接使用）
```

---

## 三、中间件部署（Docker Compose）

在 `/opt/ums/configs/` 下创建 `docker-compose.yml`：

```yaml
services:
  mysql:
    image: mysql:8.0
    container_name: ums-mysql
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}      # 生产必须强密码
      TZ: UTC
    command:
      - --character-set-server=utf8mb4
      - --collation-server=utf8mb4_0900_ai_ci
    ports:
      - "127.0.0.1:3306:3306"           # 仅本机可访问
    volumes:
      - mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      retries: 5

  rabbitmq:
    image: rabbitmq:4.3-management
    container_name: ums-rabbitmq
    restart: always
    environment:
      RABBITMQ_DEFAULT_USER: ${MQ_USER}             # 生产专用账号，禁用 guest
      RABBITMQ_DEFAULT_PASS: ${MQ_PASSWORD}
      TZ: UTC
    ports:
      - "127.0.0.1:5672:5672"           # AMQP，仅本机
      - "127.0.0.1:15672:15672"         # 管理台，仅本机（SSH 隧道访问）
    volumes:
      - rabbitmq-data:/var/lib/rabbitmq

  xxl-job-admin:                           # 可选：一期未启用调度中心可暂不部署
    image: xuxueli/xxl-job-admin:2.4.0
    container_name: ums-xxl-job
    restart: always
    environment:
      PARAMS: >-
        --spring.datasource.url=jdbc:mysql://mysql:3306/xxl_job?useUnicode=true&characterEncoding=UTF-8
        --spring.datasource.username=root
        --spring.datasource.password=${MYSQL_ROOT_PASSWORD}
    ports:
      - "127.0.0.1:8081:8080"
    depends_on:
      - mysql

volumes:
  mysql-data:
  rabbitmq-data:
```

同目录创建 `.env`（权限 600，不入版本库）：

```bash
MYSQL_ROOT_PASSWORD=<生产强密码>
MQ_USER=ums_admin
MQ_PASSWORD=<生产强密码>
```

启动与初始化：

```bash
cd /opt/ums/configs && docker compose up -d

# 1) 创建业务库
docker exec -i ums-mysql mysql -uroot -p"${MYSQL_ROOT_PASSWORD}" \
  -e "CREATE DATABASE IF NOT EXISTS cp_news DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;"

# 2) MQ 账号（若使用独立账号而非默认账号）
docker exec ums-rabbitmq rabbitmqctl list_users   # 确认无 guest 或 guest 已禁用
```

**RabbitMQ 初始化约定**：

- 预建 durable topic exchange `ums`（业务）与 `ums.dlx`（死信），可在管理台手动创建或由运维脚本创建；
- **业务队列由 Java 主服务启动时自动声明**，无需手工建队列；
- 生产环境禁用 guest 默认账号，使用最小权限专用账号；
- 消费失败本地重试 3 次（1s→2s→4s 指数退避），超限进死信队列 `ums.raw.item.fetched.dlq`。

---

## 四、应用构建与部署

### 4.1 Java 主服务（ums-main-java）

```bash
# 构建机打包（JDK 17 + Maven）
cd services/ums-main-java
mvn clean package -DskipTests
# 产物：target/ums-main-0.1.0-SNAPSHOT.jar
scp target/ums-main-0.1.0-SNAPSHOT.jar ums@<server>:/opt/ums/app/
```

**数据库迁移无需手工执行**：Flyway 在服务首次启动时自动执行 V1–V6 迁移（建表 + 信源/看板种子数据）。

### 4.2 Python 采集服务（ums-crawler-py）

```bash
# 服务器上准备虚拟环境
cd /opt/ums/app/ums-crawler-py
python3 -m venv venv
./venv/bin/pip install -r requirements.txt

# INT-03 等 JS 渲染信源需要浏览器内核（可选，磁盘约 300MB）
./venv/bin/playwright install chromium
```

- 信源选择器配置独立于代码，位于 `selectors/*.yaml`，新增信源不改代码；
- 服务提供 `GET /healthz`（健康检查）与 `POST /internal/trigger/{source_code}`（触发抓取）。

### 4.3 Web 前端（ums-web）

```bash
# 构建机执行（Node 20+）
cd services/ums-web
npm ci && npm run build
# 产物：dist/，整体上传服务器
scp -r dist ums@<server>:/opt/ums/web/
```

前端无必填环境变量，API baseURL 固定为 `/api`（同域反代，无 CORS 问题）。仅当启用公司 IAM 登录时构建前设置 `VITE_AUTH_PROVIDER=IAM`（登录页显示用户名+密码输入框）。

### 4.4 nginx 配置

`/etc/nginx/conf.d/ums.conf`：

```nginx
server {
    listen 80;
    server_name news.example.com;          # 生产域名
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl http2;
    server_name news.example.com;

    ssl_certificate     /etc/letsencrypt/live/news.example.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/news.example.com/privkey.pem;

    # 前端静态资源
    root /opt/ums/web/dist;
    index index.html;
    location / {
        try_files $uri $uri/ /index.html;   # SPA 路由回退
    }
    # 静态资源缓存
    location /assets/ {
        expires 30d;
        add_header Cache-Control "public, immutable";
    }

    # API 反代 Java 主服务
    location /api/ {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;             # AI 补加工等慢接口
    }
}
```

---

## 五、环境变量配置清单

### 5.1 Java 主服务（ums-main-java）

**数据库与 MQ（生产必须覆盖默认值）**

| 变量 | 默认值（开发） | 说明 |
|---|---|---|
| UMS_MYSQL_HOST / UMS_MYSQL_PORT | 127.0.0.1 / 3306 | MySQL 地址 |
| UMS_MYSQL_DB | cp_news | 业务库名 |
| UMS_MYSQL_USER / UMS_MYSQL_PASSWORD | root / root1234 | 生产必须改强密码 |
| UMS_MQ_HOST / UMS_MQ_PORT | 127.0.0.1 / 5672 | RabbitMQ 地址 |
| UMS_MQ_USER / UMS_MQ_PASSWORD | ums_admin / 开发默认 | 生产必须改强密码 |

**安全**

| 变量 | 说明 |
|---|---|
| UMS_JWT_SECRET | JWT 签名密钥，生产必须为 ≥32 字节随机串 |
| UMS_JWT_TTL_MINUTES | Token 有效期，默认 480（8 小时） |
| UMS_AUTH_PROVIDER | LOCAL（本地账号）或 IAM |
| UMS_ADMIN_TOKEN | 旧管理 token（已由 Spring Security JWT 接管，可留空） |

**AI 加工（智谱 GLM）**

| 变量 | 说明 |
|---|---|
| UMS_AI_API_KEY | **只走环境变量注入，不入代码/文档**；为空则 AI 加工记 SKIPPED，不影响入库 |
| UMS_AI_BASE_URL / UMS_AI_MODEL | 默认 `https://open.bigmodel.cn/api/paas/v4` / `glm-4-flash` |
| UMS_AI_ENABLED | AI 总开关，默认 true |

**内部联动**

| 变量 | 说明 |
|---|---|
| UMS_CRAWLER_URL | 采集服务地址，默认 `http://localhost:8100` |
| UMS_MGMT_URL / UMS_MGMT_USER / UMS_MGMT_PASSWORD | RabbitMQ 管理台 API（Java 侧查询队列状态用） |
| UMS_XXL_JOB_ENABLED / UMS_XXL_ADMIN / UMS_XXL_PORT / UMS_XXL_TOKEN | XXL-JOB 执行器开关与调度中心配置，默认关闭 |
| UMS_SCHEDULER_LIMIT | 单次触发最多执行的信源数，默认 10 |

### 5.2 Python 采集服务（ums-crawler-py）

| 变量 | 默认值 | 说明 |
|---|---|---|
| RABBITMQ_URL | 空 | MQ 连接串（`amqp://user:pass@host:5672/%2F`）；**空则降级 stdout 输出** |
| MQ_EXCHANGE / MQ_ROUTING_KEY | ums / raw.item.fetched | 发布目标 |
| INTERNAL_API_BASE_URL | 空 | Java 内部接口（拉取信源配置）；空则用本地 selectors YAML |
| SELECTORS_DIR | ./selectors | 选择器目录 |
| CRAWLER_HOST / CRAWLER_PORT | 0.0.0.0 / 8100 | 触发接口监听 |
| REQUEST_TIMEOUT_SEC / DEFAULT_REQUEST_INTERVAL_SEC | 20 / 2 | 请求超时 / 请求间隔（秒） |

### 5.3 Web 前端（ums-web）

| 变量 | 说明 |
|---|---|
| VITE_AUTH_PROVIDER | 可选，`IAM` 时登录页显示用户名+密码输入；默认 LOCAL |

**生产环境变量文件建议**：`/opt/ums/configs/app.env`（权限 600），由 systemd `EnvironmentFile` 加载，示例：

```bash
# --- 数据库 ---
UMS_MYSQL_HOST=127.0.0.1
UMS_MYSQL_DB=cp_news
UMS_MYSQL_USER=ums_app
UMS_MYSQL_PASSWORD=<生产密码>

# --- RabbitMQ ---
UMS_MQ_HOST=127.0.0.1
UMS_MQ_USER=ums_admin
UMS_MQ_PASSWORD=<生产密码>

# --- 安全 ---
UMS_JWT_SECRET=<32字节以上随机串>

# --- AI ---
UMS_AI_API_KEY=<智谱key，仅在此注入>

# --- 采集联动 ---
UMS_CRAWLER_URL=http://127.0.0.1:8100
UMS_MGMT_USER=ums_admin
UMS_MGMT_PASSWORD=<生产密码>
```

---

## 六、进程管理（systemd）

> 教训：**不能以 SSH 前台进程方式长期运行服务**——会话断开后进程被系统终止。生产必须用 systemd（或容器）常驻并设置开机自启。

### 6.1 Java 主服务

`/etc/systemd/system/ums-main.service`：

```ini
[Unit]
Description=UMS Main Java Service
After=network.target docker.service
Wants=docker.service

[Service]
User=ums
WorkingDirectory=/opt/ums/app
EnvironmentFile=/opt/ums/configs/app.env
ExecStart=/usr/bin/java -Xms512m -Xmx2g -jar /opt/ums/app/ums-main-0.1.0-SNAPSHOT.jar
Restart=on-failure
RestartSec=10
SuccessExitStatus=143

[Install]
WantedBy=multi-user.target
```

### 6.2 Python 采集服务

`/etc/systemd/system/ums-crawler.service`：

```ini
[Unit]
Description=UMS Crawler Python Service
After=network.target docker.service

[Service]
User=ums
WorkingDirectory=/opt/ums/app/ums-crawler-py
EnvironmentFile=/opt/ums/configs/crawler.env
ExecStart=/opt/ums/app/ums-crawler-py/venv/bin/python main.py
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
```

`crawler.env` 示例：

```bash
RABBITMQ_URL=amqp://ums_admin:<生产密码>@127.0.0.1:5672/%2F
INTERNAL_API_BASE_URL=http://127.0.0.1:8080
CRAWLER_HOST=127.0.0.1        # 仅本机触发
CRAWLER_PORT=8100
```

### 6.3 启停命令

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now ums-main ums-crawler
sudo systemctl status ums-main ums-crawler

# 重启 / 查看日志
sudo systemctl restart ums-main
journalctl -u ums-main -f --since "10 min ago"
```

---

## 七、初始化与冒烟验证

按顺序执行，全部通过后交付验收：

```bash
# 1) 中间件健康
docker ps                                  # mysql / rabbitmq 状态 Up (healthy)
curl -s http://127.0.0.1:15672/api/overview -u <user>:<pass> | head   # 管理台 API 可用

# 2) Java 主服务
curl -s http://127.0.0.1:8080/actuator/health        # {"status":"UP"}
# Flyway 已自动迁移：库中应有 V1~V6 迁移记录与 5 条信源种子
mysql -ucp_news -e "SELECT version, description FROM cp_news.flyway_schema_history ORDER BY installed_rank;"

# 3) Python 采集服务
curl -s http://127.0.0.1:8100/healthz                 # 健康检查

# 4) 触发一次采集（替换为实际信源编码）
curl -X POST http://127.0.0.1:8100/internal/trigger/CN-01
# 验证 MQ 与入库
curl -s http://127.0.0.1:8080/api/v1/news?limit=5   # 时间线返回数据

# 5) 登录链路（LOCAL 模式）
curl -X POST http://127.0.0.1:8080/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"username":"admin","password":"<密码>"}'      # 返回 JWT

# 6) 前端与反代
curl -s https://news.example.com/ | head              # 返回 index.html
curl -s https://news.example.com/api/v1/indicators | head   # 反代链路通
```

---

## 八、日常运维

### 8.1 健康巡检（建议每日）

| 检查项 | 方式 | 正常标准 |
|---|---|---|
| Java 服务 | `GET /actuator/health` | `"status":"UP"` |
| 采集服务 | `GET /healthz`（8100） | 200 |
| 容器 | `docker ps` | mysql / rabbitmq 均 Up |
| MQ 堆积 | 管理台 15672 → 队列 `ums.raw.item.fetched` | 消息数趋近 0，无持续堆积 |
| 死信 | 管理台 → `ums.raw.item.fetched.dlq` | 无新增（有则按 8.4 处理） |
| 磁盘 | `df -h` | 使用率 < 80% |
| AI 加工 | `ai_processing_log` 表 SUCCESS/FAILED 比例 | 失败率无持续升高 |

### 8.2 日志管理

| 服务 | 日志位置 |
|---|---|
| Java / Python | journald（`journalctl -u <unit>`） |
| 中间件 | `docker logs ums-mysql` / `docker logs ums-rabbitmq` |
| nginx | `/var/log/nginx/access.log`、`error.log` |

```bash
# journald 持久化（默认仅内存）/etc/systemd/journald.conf: Storage=persistent
sudo mkdir -p /var/log/journal && sudo systemctl restart systemd-journald

# nginx 日志按天切割
sudo logrotate -f /etc/logrotate.d/nginx
```

### 8.3 数据备份

```bash
# 每日 02:00 快照备份（crontab -e）
0 2 * * * docker exec ums-mysql mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" \
  --single-transaction cp_news | gzip > /opt/ums/backups/cp_news_$(date +\%F).sql.gz

# 保留 14 天
0 3 * * * find /opt/ums/backups -name "*.sql.gz" -mtime +14 -delete
```

恢复演练（每季度至少一次）：`gunzip < cp_news_xxx.sql.gz | docker exec -i ums-mysql mysql -uroot -p cp_news`。

### 8.4 MQ 运维与死信重放

- 采集失败的消息经 3 次重试后进入 `ums.raw.item.fetched.dlq`；
- 排查原因（信源变更、WAF 拦截、消息格式）后，用 `services/ums-main-java/scripts/replay_dlq.py` 重放到 exchange `ums`（routing key `raw.item.fetched`），消息将重新走入库+AI 加工链路；
- 该脚本经 15672 管理 API 操作，运行前确认脚本内管理台账号与生产一致。

### 8.5 采集调度管理

- **一期推荐方式**：XXL-JOB 调度中心配置每日 1 次任务（HTTP 任务类型调用 `POST http://<java>/internal/trigger/{source_code}`），或系统 cron + curl 触发；
- **手动补采**：`POST /internal/trigger/{source_code}`，一次只触发一个信源；
- **信源上下线**：管理后台「信源管理」启停，或 `POST /api/v1/admin/sources` 新增；
- 采集任务执行记录在管理后台「采集任务」页（`cp_collection_task` 表）查看成败与耗时。

### 8.6 公司 IAM 接入（切换登录方式）

系统预留了 IAM 直连模式，**无需改代码**，配置环境变量即可：

```bash
# /opt/ums/configs/app.env 中追加
UMS_AUTH_PROVIDER=IAM            # 后端默认认证方式
UMS_IAM_ENABLED=true
UMS_IAM_AUTH_API_URL=https://iam.company.com/api/auth/verify   # 公司 IAM 真实地址
UMS_IAM_AUTH_MODE=API_KEY        # API_KEY | BEARER | NONE
UMS_IAM_API_KEY=<iam密钥>
UMS_IAM_AUTO_PROVISION=true      # 首次登录自动建 cp_user
UMS_IAM_DEFAULT_ROLES=USER
```

前端需以 `VITE_AUTH_PROVIDER=IAM` 重新构建。字段口径详见 `.env.iam.example`。未接入前 `UMS_AUTH_PROVIDER=LOCAL`（admin 本地账号登录）。

### 8.7 AI 链路验证脚本

`services/ums-main-java/scripts/verify_ai.sh`：一键验证 AI 加工链路（重启服务带 key → 补加工 → 轮询 → 输出 `ai_processing_log` 统计/摘要样例/队列状态）。**因涉及 API key，请在受控终端以参数或 `UMS_AI_API_KEY` 环境变量传入运行，不要把 key 写入任何文件。**

---

## 九、故障排查手册

| 现象 | 可能原因 | 处置 |
|---|---|---|
| Python 服务无消息入库 | `RABBITMQ_URL` 未配置 | 属降级模式（stdout 输出）。配置连接串后重启服务 |
| Java 启动报 MQ 连接失败 | RabbitMQ 未启动/凭据错误 | `docker ps` 确认容器；核对 UMS_MQ_*；紧急情况可加 `--spring.rabbitmq.listener.simple.auto-startup=false` 先启动服务（跳过消费者） |
| 服务重启后消失 | 以前台/SSH 会话方式启动 | 改用 systemd 常驻（第六节），勿在 SSH 会话直接 `java -jar` 长跑 |
| 某信源持续采集失败 | 站点改版 / TLS 指纹 WAF 拦截 | WAF 拦截（如 INT-01 类）**不做对抗性绕过**；改版则更新 `selectors/*.yaml`；JS 渲染站点放宽超时（Playwright 已按 60s 配置） |
| 死信队列增长 | 消息格式异常或信源故障 | 8.4 节排查后重放；重放仍失败则修数据后丢弃 |
| 登录 401 且带 409 语义 | 异常链吞状态码 | 确认部署版本含 `GlobalExceptionHandler`（@RestControllerAdvice 全局异常处理） |
| AI 加工全 SKIPPED | `UMS_AI_API_KEY` 为空 | systemd `EnvironmentFile` 是否加载；确认 key 有效 |
| 前端刷新 404 | nginx 缺 SPA 回退 | `try_files $uri $uri/ /index.html` |
| 管理接口 401 | JWT 过期或角色不足 | 重新登录；RBAC 需 ADMIN 角色 |
| MQ 磁盘告警 | 持久化消息堆积 | 管理台查消费者在线状态；必要时清理历史消息 |
| 服务器存在 erl_crash.dump | RabbitMQ 曾异常退出 | 排查 `docker logs ums-rabbitmq`；确认后删除该转储文件 |

---

## 十、升级与回滚

### 10.1 发布流程

```bash
# 1) 构建并上传新版本（保留旧包）
mvn clean package -DskipTests
scp target/ums-main-0.1.0-SNAPSHOT.jar ums@<server>:/opt/ums/app/ums-main.new.jar

# 2) 服务器上替换（先停服务，数据库迁移由新版本启动时 Flyway 自动执行）
sudo systemctl stop ums-main
mv /opt/ums/app/ums-main.new.jar /opt/ums/app/ums-main-0.1.0-SNAPSHOT.jar
sudo systemctl start ums-main

# 3) 冒烟：/actuator/health + 时间线接口 + flyway_schema_history 新版本记录
```

### 10.2 回滚要点

- **应用回滚**：恢复旧 jar 重启即可（Flyway 已迁移的表结构向前兼容时安全）；
- **含数据库迁移的回滚**：Flyway 不自动回滚 SQL。发布前必须执行 `mysqldump` 快照（见 8.3），回滚时恢复快照，**先停 Java 服务再恢复**，避免状态错乱；
- **前端回滚**：`/opt/ums/web/dist` 发布前整体备份（`cp -r dist dist.bak.<日期>`），回滚即目录对调。

### 10.3 版本管理约定

- 代码仓库打版本标签（如 `v1.0.x`），jar 与 dist 一同归档到备份目录；
- 数据库迁移脚本只增不改（Flyway checksum 校验会阻止修改已执行脚本）。

---

## 十一、上线前检查清单

- [ ] 安全组仅放行 22 / 80 / 443，3306/5672/8081/8100/9999 仅内网；
- [ ] MySQL / RabbitMQ 生产强密码，禁用 guest 账号；
- [ ] `UMS_JWT_SECRET` 已替换为生产随机串（≥32 字节）；
- [ ] `UMS_AI_API_KEY` 仅通过 systemd EnvironmentFile 注入，未出现在仓库/文档；
- [ ] IAM 地址确认后 `UMS_AUTH_PROVIDER=IAM`，或明确使用 LOCAL 并修改 admin 初始密码；
- [ ] nginx 托管 dist + `/api` 反代 + SPA 回退 + SSL 证书生效；
- [ ] Flyway 迁移完整（V1–V6），种子数据就位；
- [ ] XXL-JOB / cron 每日采集任务启用；
- [ ] 备份 crontab 生效并完成一次恢复演练；
- [ ] 全链路冒烟（采集 → 入库 → AI → 前端三入口 → 管理后台）无 P0/P1 遗留缺陷。

---

> 本文档为部署运维基线；架构、端口或中间件变更须经评审后更新版本号并同步相关文档。
